# New PhaseRegion-primary capillary graph route.
#
# This wrapper deliberately does not invoke experiment/run.py.  It is consumed
# by experiment/ch14/diagnose_phase_region_capillary_graph_steps.py, whose route
# is:
#
#   eta -> q_l = Q_h(eta)
#   q_g = |C| - q_l
#   PhaseRegionBatch(GAS_ABOVE graph)
#   E_h second-variation modal step
#
# The base config supplies the physical SI parameters, grid, and initial graph.

base_config: ../ch14_capillary.yaml

experiment:
  type: phase_region_capillary_graph

phase_region_graph:
  owner: gas_above
  chart: graph_periodic
  q_measurement: exact_graph_column_integral
  phase_owner_map: exact_complement
  energy: graph_perimeter_second_variation
  dynamics: velocity_verlet_linear_capillary_mode
  pressure_velocity: not_connected
  force_admissible: false
  backend:
    use_gpu: true
  run:
    periods: 1.0
    steps_per_period: 2560
