% paper/preamble.tex
%% silence パッケージ除去（XeLaTeX + CJK 非互換）

% -- 付録セクション番号を Z 以降も AA,AB,... に拡張 --
\usepackage{alphalph}
\makeatletter
\renewcommand{\@Alph}[1]{\AlphAlph{#1}}
\makeatother

% -- Fonts --
\setmainfont{Times New Roman}
\setsansfont{Helvetica Neue}

% -- Japanese fonts: suppress expected warnings during zxjafont loading --
%    zxjafont W2: W2 not installed, W3 substitution is correct behavior
%    xeCJK redef: expected when zxjafont overrides bxjsarticle defaults
\ExplSyntaxOn
\msg_redirect_name:nnn { xeCJK } { CJKfamily-redef } { none }
\ExplSyntaxOff
\makeatletter
\let\zxjf@save@PkgWarnNL\PackageWarningNoLine
\renewcommand{\PackageWarningNoLine}[2]{%
  \def\zxjf@tmpa{zxjafont}\edef\zxjf@tmpb{#1}%
  \ifx\zxjf@tmpa\zxjf@tmpb\else\zxjf@save@PkgWarnNL{#1}{#2}\fi}
\usepackage[hiragino-pron]{zxjafont}
\let\PackageWarningNoLine\zxjf@save@PkgWarnNL
\makeatother
% -- Japanese fonts have no italic; map to upright shape --
\DeclareFontShape{TU}{HiraginoMinchoProNW3(0)}{m}{it}{<->ssub*HiraginoMinchoProNW3(0)/m/n}{}

% -- Section always starts on new page (skip if immediately after \part) --
\usepackage{etoolbox}
\newbool{afterpart}
\AtBeginDocument{
  \preto{\part}{\booltrue{afterpart}}
  \preto{\section}{%
    \ifbool{afterpart}{\boolfalse{afterpart}}{\clearpage}%
  }
}

% -- Algorithm environments --
\usepackage[linesnumbered,ruled,vlined,norelsize]{algorithm2e}

% -- Math / Figures / Tables --
\usepackage{amsmath, amssymb, amsthm, mathtools}
\usepackage{bm}
\usepackage{booktabs, array, multirow, tabularx}
\usepackage{graphicx}
\usepackage{float}
\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, theorems}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, backgrounds, calc, decorations.pathreplacing, decorations.pathmorphing, matrix, patterns}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage[plainpages=false,pdfpagelabels,hypertexnames=false]{hyperref}
\usepackage{cleveref}
\usepackage{enumitem}
\usepackage{etoolbox}
\usepackage{ragged2e}
% -- Tell caption that bxjsarticle behaves like a standard class --
\makeatletter\def\caption@documentclass{standard}\makeatother
\usepackage[
  justification=centering,
  singlelinecheck=true,
  font={small,stretch=0.94},
  labelfont=bf,
  skip=4pt
]{caption}
\captionsetup[figure]{position=bottom}
\captionsetup[table]{position=top}
\usepackage{subcaption}
\usepackage{microtype}
\usepackage{nicefrac}

% -- Unified float/table typography --
\newcommand{\PaperTableArrayStretch}{1.12}
\newcommand{\PaperTableDenseArrayStretch}{1.02}
\newcommand{\PaperTableSetup}{%
  \small
  \setlength{\tabcolsep}{4pt}%
  \renewcommand{\arraystretch}{\PaperTableArrayStretch}%
}
\newcommand{\PaperTableDenseSetup}{%
  \footnotesize
  \setlength{\tabcolsep}{3pt}%
  \renewcommand{\arraystretch}{\PaperTableDenseArrayStretch}%
}
\newcommand{\PaperTableExtraDenseSetup}{%
  \scriptsize
  \setlength{\tabcolsep}{3pt}%
  \renewcommand{\arraystretch}{0.92}%
}
\AtBeginEnvironment{table}{\centering\PaperTableSetup}

% -- Page layout --
\geometry{reset, a4paper, top=25mm, bottom=28mm, left=26mm, right=26mm, headheight=16pt, footskip=20pt}
\raggedbottom
\emergencystretch=2em
\setlength{\hfuzz}{3pt}
\hbadness=4000

% -- Colors (Merged from A and B) --
\definecolor{navy}{RGB}{0,40,90}
\definecolor{blue2}{RGB}{0,80,160}
\definecolor{midblue}{RGB}{0,90,170}
\definecolor{lightblue}{RGB}{230,242,255}
\definecolor{tblue}{RGB}{218,234,255}
\definecolor{tgreen}{RGB}{215,255,225}
\definecolor{tyellow}{RGB}{255,250,210}
\definecolor{tred}{RGB}{255,225,220}
\definecolor{darkgray}{RGB}{60,60,60}
\definecolor{boxgray}{RGB}{245,245,245}
\definecolor{green4}{RGB}{0,120,60}
\definecolor{red2}{RGB}{160,0,0}
\definecolor{orange2}{RGB}{200,90,0}
\definecolor{torange}{RGB}{255,238,210}
\definecolor{teal2}{RGB}{0,110,110}
\definecolor{tteal}{RGB}{210,245,245}
\definecolor{purple2}{RGB}{100,0,140}
\definecolor{tpurple}{RGB}{240,220,255}
\definecolor{brown2}{RGB}{120,60,0}
\definecolor{tbrown}{RGB}{250,235,215}

% -- tcolorbox styles (Preserving A's rich styling) --
\tcbset{
  mybox/.style={
    enhanced, breakable,
    colback=tblue, colframe=blue2,
    toprule=0.8pt, bottomrule=0.8pt,
    leftrule=0.8pt, rightrule=0.8pt,
    fonttitle=\bfseries\small\color{white},
    colbacktitle=navy,
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={sharp corners},
    sharp corners=south,
  },
  derivbox/.style={
    enhanced, breakable,
    colback=boxgray, colframe=navy!40,
    toprule=0.5pt, bottomrule=0.5pt,
    leftrule=3pt, rightrule=0.5pt,
    sharp corners,
  },
  resultbox/.style={
    enhanced, breakable,
    colback=tgreen, colframe=navy,
    toprule=1pt, bottomrule=1pt,
    leftrule=1pt, rightrule=1pt,
    sharp corners,
  },
  warnbox/.style={
    enhanced, breakable,
    colback=tyellow, colframe=red2!70,
    toprule=0.6pt, bottomrule=0.6pt,
    leftrule=3pt, rightrule=0.6pt,
    sharp corners,
  },
  algbox/.style={
    enhanced, breakable,
    colback=white, colframe=navy,
    toprule=1pt, bottomrule=1pt,
    leftrule=1pt, rightrule=1pt,
    sharp corners,
    fonttitle=\bfseries\small\color{white},
    colbacktitle=navy,
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={sharp corners},
  },
  defbox/.style={
    enhanced, breakable,
    colback=boxgray, colframe=darkgray!50,
    toprule=0.6pt, bottomrule=0.6pt,
    leftrule=0.6pt, rightrule=0.6pt,
    sharp corners,
  },
}

% -- Header/Footer --
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{navy}二次元気液混合流体の数値計算法（統合版）}
\fancyhead[R]{\small\color{navy!70}CLS + CCD + RC + PPE}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% -- Section headings --
\titleformat{\section}{\large\bfseries\color{navy}}{\thesection.}{0.8em}{}[\color{blue2}\rule{\linewidth}{0.8pt}]
\titleformat{\subsection}{\normalsize\bfseries\color{navy!80}}{\thesubsection}{0.8em}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{navy!60}}{\thesubsubsection}{0.8em}{}
\titleformat{\paragraph}[runin]{\normalsize\bfseries\color{navy!60}}{}{1em}{}

% -- Math macros --
\newcommand{\jp}[1]{#1} % Compatibility wrapper for old macro
\newcommand{\pd}[2]{\dfrac{\partial #1}{\partial #2}}
\newcommand{\pdd}[2]{\dfrac{\partial^2 #1}{\partial #2^2}}
\newcommand{\pdm}[3]{\dfrac{\partial^2 #1}{\partial #2\,\partial #3}}
\newcommand{\bu}{\bm{u}}
\newcommand{\bg}{\bm{g}}
\newcommand{\bn}{\bm{n}}
\newcommand{\bx}{\bm{x}}
\newcommand{\bnabla}{\bm{\nabla}}
\newcommand{\He}{H_\varepsilon}
\newcommand{\de}{\delta_\varepsilon}
\newcommand{\eps}{\varepsilon}
\newcommand{\eff}{\mathrm{eff}}
\newcommand{\Ord}[1]{\mathcal{O}(#1)}
\newcommand{\half}{\tfrac{1}{2}}
\newcommand{\fp}[1]{f^{(1)}_{#1}}
\newcommand{\fpp}[1]{f^{(2)}_{#1}}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}

% -- SP-O rewrite (2026-04-23) operator-family shorthands --
%    \ensuremath lets each macro work in both math and text contexts.
\newcommand{\CCD}{\ensuremath{\mathrm{CCD}}}
\newcommand{\FCCD}{\ensuremath{\mathrm{FCCD}}}
\newcommand{\UCCD}[1][]{\ensuremath{\mathrm{UCCD}#1}}
\newcommand{\DCCD}{\ensuremath{\mathrm{DCCD}}}
\newcommand{\Ridge}{\ensuremath{\text{Ridge}}}
\newcommand{\EikSolve}{\ensuremath{\mathrm{Eik}}}
\newcommand{\HFE}{\ensuremath{\mathrm{HFE}}}
\newcommand{\GFM}{\ensuremath{\mathrm{GFM}}}
\newcommand{\FaceJet}[1]{\ensuremath{\mathcal{J}_{f}\!\left(#1\right)}}
\newcommand{\BF}{\ensuremath{\mathrm{BF}}}
\newcommand{\DC}{\ensuremath{\mathrm{DC}}}

% -- ξ disambiguation: §6 index / §3.4 ridge auxiliary / §6.2 grid density --
\newcommand{\xiidx}{\xi_{\text{idx}}}
\newcommand{\xiridge}{\xi_{\Ridge}}
\newcommand{\omegagrid}{\omega_{\text{grid}}}

% -- FCCD face coefficient shorthand: β_f ≡ (1/ρ)_f harmonic-averaged --
\newcommand{\betaf}{\beta_{f}}

% -- A3 traceability margin note (equation → discretisation → code → test) --
% Quiet by default; \Athreedebug is a numeric flag (1 → emit margin notes).
\providecommand{\Athreedebug}{0}
\newcommand{\Athree}[4]{%
  \ifnum\Athreedebug=1%
    \marginpar{\scriptsize A3: \ref{#1}\\ #2\\ \texttt{#3}\\ \texttt{#4}}%
  \fi%
}

% -- Theorem environments --
% amsthm の plain スタイルは \itshape を使うが，
% ヒラギノ明朝にイタリック体がないため警告が出る．
% definition スタイル（\normalfont）に統一する．
\theoremstyle{definition}
\newtheorem{remark}{Remark}
\newtheorem{definition}{定義}
\newtheorem{theorem}{定理}
\newtheorem{corollary}[theorem]{系}
\newtheorem{proposition}[theorem]{命題}

% -- Hyperlinks --
\hypersetup{
  colorlinks=true,
  linkcolor=navy,
  citecolor=navy,
  urlcolor=blue2,
  pdftitle={Two-Dimensional Gas-Liquid Two-Phase Flow Numerical Methods (Merged)},
}
