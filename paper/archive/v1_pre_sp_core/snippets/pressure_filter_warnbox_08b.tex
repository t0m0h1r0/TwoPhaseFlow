%!TEX root = ../../../main.tex
%
% §8b 08b_pressure.tex 内の "圧力場へのフィルタ適用禁止" warnbox + 代替処理 enumerate
%   退避: CHK-193 Phase D-3（2026-04-24）
%
% 退避理由:
%   §8.5 08c_pressure_filter.tex の SP-J §4 完全版
%   （sec:dccd_pressure_filter_prohibition, eq:dccd_filter_bias,
%    eq:dccd_filter_symmetric_allowed, 安全性マトリクス, 既定禁止ポリシー）と
%   内容が完全重複していた．§8b の statement は §8c 拡張時の旧統合残滓．
%
% 本編の代替:
%   paper/sections/08b_pressure.tex L107-144 を 2 行の cross-ref に圧縮．
%   外部参照（11e_pressure_solver x2, 11g_summary）のため
%   sec:dccd_pressure_nofilt は phantomsection 化．
%
% 再活性化条件:
%   §8c の完全版を取り下げ，§8b に inline warnbox を復活する設計へ戻す場合．

% ─────────────────────────────────────────────────────────────────────────────
% 圧力場へのフィルタ適用禁止（旧 08c_pressure_filter.tex から統合）
% ─────────────────────────────────────────────────────────────────────────────
\begin{tcolorbox}[warnbox, title={圧力場へのフィルタ適用禁止}]
\label{sec:dccd_pressure_nofilt}%
\label{warn:pressure_direct_filter}%
PPE ソルバー（§~\ref{sec:ppe_solve}）に進む前に，
フィルタ適用に関する重要な制約を明確にしておく．
§~\ref{sec:dccd_filter_theory} で導入した DCCD フィルタを圧力場に直接適用してはならない．

圧力場 $p$ に対する直接的なフィルタ適用は，
速度修正ステップ $\bu^{n+1} = \bu^* - (\Delta t/\rho^{n+1})\,\bnabla(\delta p)$（§~\ref{sec:pressure}）が
\textbf{採用した離散射影条件}を満たすように構成されているためである．
本稿では PPE 右辺に Rhie--Chow/DCCD 正則化済みの発散を用いるため，
射影でゼロにする発散演算子もその正則化を含む離散演算子として解釈する
（§~\ref{sec:dccd_decoupling}）．
PPE 求解後に $p$ を直接書き換えると，この解いた線形系と速度補正の対応が崩れる．

圧力関連ノイズへの対処が必要な場合，以下の代替処理が保全性を保ちながら有効である：
\begin{enumerate}
  \item \textbf{圧力勾配のフィルタリング：}
        NS 右辺に現れる $\bnabla p$（CCD 微分として評価）に対して DCCD フィルタを適用する．
        $p$ 自体は書き換えないため，PPE の線形系そのものは保持される．
  \item \textbf{PPE 右辺の正則化：}
        ポアソン方程式 $\bnabla\cdot(1/\rho\,\bnabla(\delta p)) = \bnabla^{\mathrm{RC}}\cdot\bu^*/\Delta t$ の
        右辺 $s_h = \bnabla^{\mathrm{RC}}\cdot\bu^*/\Delta t$（式~\eqref{eq:rc_divergence}）に
        軽微なフィルタを適用して $p$ 求解前に平滑化する．
        これはポアソンソルバーの収束安定化にも寄与し，
        §~\ref{sec:ppe_pseudotime} の擬似時間ソルバーでは
        反復減衰によって自然に実現される．
\end{enumerate}

\noindent\textbf{実装上の禁忌：}
\texttt{p[:,:] = filter(p)} の形で圧力配列を直接書き換えることは，
発散自由射影直後に誤差を混入させ，次タイムステップの速度補正を破壊する．
本稿の擬似時間 PPE ソルバーおよび速度補正モジュールでは
この操作を一切行っていないことを確認済みである．
\end{tcolorbox}
