% paper/sections/12u12_ao_capillary_split_gate.tex
%
% U12: PhaseRegion capillary acceptance split gate
% ═══════════════════════════════════════════════════════════════════════════════
\subsubsection{U12：PhaseRegion 毛管経路の事前ゲート}
\label{sec:U12_ao_capillary_split_gate}

\paragraph{目的．}
U12 は，PhaseRegion 毛管経路を第~\ref{sec:verification}章以降の
標準構成へ混入してよいかを判定する事前ゲートである．
ここで検証するのは計算手順の性能ではなく，
式~\eqref{eq:capillary_pressure_reaction_projection} の
圧力反力部分空間 $R_p$ を定義しないまま毛管残差を読む論理が破綻すること，
式~\eqref{eq:ao_graph_hfe_jump}--\eqref{eq:ao_graph_hfe_face_law} の
$q$ 所有グラフ HFE ジャンプが現在の界面から構成されること，
PhaseRegion グラフチャートの測度と HFE 候補が現在界面から構成されること，
および式~\eqref{eq:grid_regular_stratum_axis}--\eqref{eq:grid_regular_stratum_shift} の
正則層ガードが接線方向格子を壊さないことである．
したがって U12 は成功した物理ベンチではなく，標準経路を守るための
単体ゲートとして読む．

\paragraph{採用境界の検査根拠．}
毛管波を標準構成へ接続するには，
全圧力像への過射影を棄却するだけでは足りない．
現在グラフから作る HFE ジャンプ，滑らかな圧力履歴座標，
HFE 受理条件，および追随格子の正則層ガードを同時に満たす必要がある．
そこで U12 では，毛管波 N32/N64 の代数反例に加え，
非一様 $x$ 格子上の式~\eqref{eq:ao_graph_hfe_jump} の符号・保存性，
グラフ HFE ジャンプを圧力履歴座標へ保存しないこと，
微小 $dt$ でも空間毛管駆動があれば HFE を受理すること，
ほぼ水平界面で $x$ 方向を縮めないことをゲートに加える．

\paragraph{代数反例．}
固定アクティブ集合上で，節点幾何エネルギー勾配を $g$，
アクティブ制約ヤコビアンを $J_q$，SPD 計量を $W$ とする．
毛管 Riesz 代表と全セル圧力反力を同じ Schur 系
式~\eqref{eq:ao_full_pressure_cancellation} から作ると，
両者は同じ解を持つため，面上の 釣合い駆動 は厳密に $0$ になる．
このため，節点 Young--Laplace 残差 $e=g+J_q^T\pi$ が非零であることは，
面速度空間で実際に仕事をする毛管加速度の証明にならない．
PhaseRegion 毛管経路を物理経路として読むには，全圧力像ではなく
PPE 互換の $R_p(q_T)$ を先に定義し，
$r_\sigma-\Pi^{M_f}_{R_p}r_\sigma$ を直接評価する必要がある．

\paragraph{観測結果．}
表~\ref{tab:U12_ao_gate} に U12 のゲート結果を示す．
平坦界面では全経路がゼロ駆動でなければならない．
一方，毛管波では全圧力像への過射影が厳密分解で
駆動を消すこと，成分体積 Hodge 診断が非静的波を検出すること，
現在グラフから作る HFE ジャンプを圧力履歴として再利用しないこと，
HFE 受理条件が $dt$ 倍された微小増分ではなく空間駆動で判定されること，
および追随格子の正則層ガードが支配法線方向だけを動かすことを確認する．

\begin{table}[htbp]
\caption{U12：PhaseRegion 毛管経路ゲートの観測結果}
\label{tab:U12_ao_gate}
\centering
\PaperTableDenseSetup
\begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}p{0.19\linewidth}>{\raggedright\arraybackslash}p{0.25\linewidth}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}p{0.18\linewidth}@{}}
\toprule
ケース & 判定対象 & 観測値 & 判定 \\
\midrule
平坦界面 & ゼロ駆動条件 & 厳密分解と成分体積 Hodge 診断はともに $0$．平坦・静的条件ではゼロ駆動と読む． & \checkmark \\
毛管波 N32 & 全圧力像の反例 & 厳密全圧力分解は釣合い駆動 $0$．成分体積 Hodge 診断は釣合いノルム $2.1176$． & \checkmark 診断 \\
毛管波 N64 & 解像度を変えた非静的診断 & 厳密全圧力分解は釣合い駆動 $0$．成分体積 Hodge 診断は釣合いノルム $2.3055$． & \checkmark 診断 \\
PhaseRegion グラフ HFE & 現在界面ジャンプ候補 & 非一様 $x$ 格子上で $j_{gl}^{G}=-(\sigma/\Delta x_i)\partial S_G/\partial H_i$ を評価し，$\|j_{gl}^{G}\|_\infty=11.5323$，$\left|\sum_i j_i\Delta x_i/\sum_i\Delta x_i\right|=1.30\times10^{-16}$，波頂ジャンプは負． & \checkmark \\
履歴・受理 & HFE と圧力履歴座標の分離 & グラフ HFE ジャンプは圧力履歴座標へ再利用されず，HFE は $dt=10^{-8}$ で増分ノルム $10^{-14}$ でも空間駆動 $10^{-6}$ により受理． & \checkmark \\
正則層ガード & 追随格子 & $|\phi|<\phi_{\min}$ の節点で最大 $|\partial_a\phi|$ の座標線だけを動かす．ほぼ水平な毛管波で $x$ 変位 $0$，$y$ 最大変位 $2.50\times10^{-3}$． & \checkmark \\
未確定分解境界 & 不受理条件 & 未確定の近似分解は合格扱いせず停止し，N32/N64 毛管波の非静的駆動を $2.40\times10^{-5}/2.32\times10^{-5}$ として保持する． & \checkmark 境界 \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.96\linewidth]{figures/ch12_u12_ao_capillary_split_gate.pdf}
\caption{U12：PhaseRegion 毛管経路ゲート．左は厳密全圧力分解と
成分体積 Hodge 診断の釣合い駆動，中央は未確定分解を不受理にして
非静的駆動を消さないこと，右はグラフ HFE と追随格子の受理条件を示す．}
\label{fig:U12_ao_gate}
\end{figure}

\paragraph{出口条件．}
U12 から第~\ref{sec:verification}章へ渡す結論は次の 5 点である．
第一に，全圧力像へ射影する分解と，
式~\eqref{eq:capillary_pressure_reaction_projection} で $R_p$ を定義する
PhaseRegion 毛管経路を同じ経路として扱わない．
第二に，PhaseRegion 毛管経路の精度は式~\eqref{eq:ao_fast_error_budget} の
共変量誤差と射影誤差で明示されなければならない．
第三に，PhaseRegion グラフチャートでは式~\eqref{eq:ao_graph_hfe_jump}--\eqref{eq:ao_graph_hfe_face_law}
の現在グラフ HFE ジャンプを使い，前格子の圧力ジャンプ履歴を使わない．
第四に，追随格子では式~\eqref{eq:grid_regular_stratum_axis}--\eqref{eq:grid_regular_stratum_shift}
の正則層ガードを満たし，接線方向格子幅を界面退避の副作用で壊さない．
第五に，$R_p$ 未確定の近似分解，式~\eqref{eq:ao_projection_face_bridge}
の Hodge 逆適用済み面補間を満たさない分解，圧力履歴座標を持たない分解，
および式~\eqref{eq:ao_face_cochain_regrid} の移動格子面量移送を欠く分解は
標準経路へ入れない．
このため，第~\ref{sec:benchmarks}章の毛管波結果は
U12 が棄却した全圧力像分解の成功証明ではない．
第~\ref{sec:ao_fast_state_space}節の最終契約を満たす経路だけを
PhaseRegion 毛管経路として読む．
