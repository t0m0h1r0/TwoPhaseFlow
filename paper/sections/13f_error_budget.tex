% paper/sections/13f_error_budget.tex
%
% V1-V10 横断の精度バジェット総合
% Hosts labels: sec:error_budget, sec:accuracy_summary
% (tab:accuracy_summary 09f, sec:time_accuracy_table 07 を authoritative とし
%  本節からは hosting せず外部参照のみで連結する)
% ═══════════════════════════════════════════════════════════════════════════════
\subsection{精度バジェット：V1--V10 誤差寄与の統合評価}
\label{sec:error_budget}
\label{sec:accuracy_summary}

\paragraph{構成．}
本節は V1--V10 横断の精度結果を一表に集約し，
\textbf{各誤差源} (空間離散，時間離散，界面表現，曲率推定，CLS 再初期化) と
\textbf{各設定パラメータ} ($\rho_r$, $\alpha$, $\Delta t$, $h$) の関係を整理する
（観測収束次数の Richardson 外挿運用は~\cite{Roy2003}）．
過去の表の値は用いず，V1--V10 の再生成結果に基づいて整理する．

\paragraph{Master accuracy summary．}

\begin{table}[!ht]
\caption{V1--V10 master accuracy summary．
slope は log-log fitting (3 点以上)；単一値はその設定での代表値．
括弧内は主な誤差源．}
\label{tab:v_accuracy_summary}
\centering\scriptsize
\begin{tabular}{l l l l l}
\toprule
ID & 検証対象 & 主要指標 & 観測値 & 設計次数 \\
\midrule
V1-a & 単相 TGV 空間 & $E_{\mathrm{rel}}$ at $N=64,128$ & $2.4\times 10^{-9}$ (時間誤差床) & — \\
V1-b & 単相 TGV 時間 (AB2/IMEX-BDF2) & slope($E_{\mathrm{rel}}, \Delta t$) & $\mathbf{2.00}$ & $O(\Delta t^2)$ \checkmark \\
V2-a & Kovasznay CCD 空間残差 & slope($\|R\|_\infty, h$) & $\mathbf{6.00}$ & $O(h^6)$ \checkmark \\
V3   & 静止液滴 long-term ($n=200$) & $|\Delta p_{\mathrm{rel}}|$ at $N=128$ & $0.97\%$ & — \\
V4   & Galilean offset 残差 & $\|\Delta u\|_\infty^{\mathrm{end}}$ & $0.21$ (壁境界・参照点ゲージ支配) $\triangle$ & — \\
V5   & 寄生流れ multistep (CCD/FD) & CCD/FD 比 at $\rho_r=1, N=32$ & $1/5.9$ & — \\
V6   & 密度比 sweep ($\rho_r=2..833$) & $|\Delta p_{\mathrm{rel}}|^{\max}$ & $1.77\%$ ($\rho_r=833, N=32$) \checkmark & — \\
V7   & IMEX-BDF2 二相時間 & slope($\|e\|_\infty, \Delta t$) & $\mathbf{0.56}$ ($\times$ 設計未達) & $O(\Delta t^2)$ 設計 \\
V8   & 非一様 NS 静止 ($\alpha=2$) & $|\Delta p_{\mathrm{rel}}|$ at $N=96$ & $2.6\%$ \checkmark & — \\
V9   & local $\varepsilon$ (§14) & C/A at $N=32$ & $0.11\times$ ($\checkmark$) & — \\
V10-a& Zalesak ($\alpha=1$) & volume drift at $N=128$ & $0.107\%$ \checkmark & — \\
V10-b& Zalesak ($\alpha=2$) & volume drift / centroid order & $1.519\%$ / $0.34$ ($\triangle$) & — \\
V10-c& 単一渦 reversal ($\alpha=2$) & $L^1$ reversal / volume drift & $2.424\times10^{-3}$ / $0.000\%$ \checkmark & — \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{誤差源スタック．}
\begin{enumerate}\setlength{\itemsep}{0pt}
  \item \textbf{空間離散誤差}．CCD 6 次（V2 で確証）．Kovasznay slope $6.00$，TGV では時間誤差床下．
  \item \textbf{時間離散誤差}．単相 NS は AB2/IMEX-BDF2 で $O(\Delta t^2)$（V1 slope $2.00$）．
        二相結合では界面再初期化 + 曲率の陽的遅れ評価で実効 $O(\Delta t^{0.56})$（V7; \autoref{sec:imex_bdf2_twophase_time}）．
  \item \textbf{界面表現誤差}．Heaviside--$\delta$ の local $\varepsilon$ は
        FCCD/HFE/pressure-jump PPE と同時に使う条件で有効（V9）；
        旧 CCD/CSF 経路の結果は §14 stack の根拠から除外する．
  \item \textbf{曲率推定誤差}．V5 では CSF $\kappa$ + CCD 勾配が全表ケースで FD 以下となり，
        粗格子・低密度比ほど寄生流れの抑制が大きい．
  \item \textbf{CLS 形状追跡誤差}．FCCD/TVD--RK3 では非一様格子 $\alpha=2$ の volume drift は
        $1.519\%$ まで収まるが，Zalesak slot の centroid order は $0.34$ に留まる（V10）．
\end{enumerate}

\paragraph{合格判定要約．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \checkmark \textbf{合格}: V1-b (時間 2 次)，V2-a (空間 6 次)，
        V3 (BF 平衡)，V5 (CCD 寄生流れ抑制)，V6 (密度比ロバスト)，
        V8 (非一様静止), V10-a ($\alpha=1$), V10-c (単一渦 reversal).
  \item $\triangle$ \textbf{条件付き合格}: V4 (固定 Eulerian + 参照点固定 PPE で
        $O(\|\bm U\|)$ 残差), V10-b ($\alpha=2$ Zalesak: volume drift $1.519\%$ だが centroid order $0.34$).
  \item $\times$ \textbf{要改善 / 推奨せず / 制約条件}:
        V7 (二相時間 slope $0.56$ → 設計 $O(\Delta t^2)$ 未達; 再初期化頻度差 + $\kappa$ 陽的遅れ評価が律速).
  \item \checkmark \textbf{§14 stack 条件付き合格}: V9
        (local $\varepsilon$ は $N=32$ で C/A=$0.11$，C/B=$0.15$；
        HFE curvature + pressure-jump PPE と同時採用する).
\end{itemize}

\paragraph{設計指針 (PR-5 Algorithm Fidelity)．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item 単相 NS：CCD 6 次空間 + AB2/IMEX-BDF2 2 次時間が paper-exact 設計通り．
  \item 二相 NS：分相 PPE + DC + HFE 構成が密度比 $\le 833$ でロバスト（V6）．
        ただし時間方向の実効次数は界面処理に律速されるため，
        \textbf{$\Delta t$ を $h_{\min}$ より小さく取る安全ファクタ}（CFL $\le 0.4$）が推奨．
  \item 非一様格子：$\alpha > 1$ でも FCCD/TVD--RK3 の体積保存は成立するが，
        Zalesak slot のような薄い幾何特徴では重心追跡が低次化する．
        local $\varepsilon$ は §14 stack と同時に採用し，2D snapshot 診断で hot spot を確認する．
  \item 寄生流れ抑制：CCD は V5 の全表ケースで FD 以下であり，
        代表的には $\rho_r=1,N=32$ で FD の $1/5.9$，
        $\rho_r=100,N=64$ で $1/2.5$．高解像度・高密度比では差が縮小する．
\end{itemize}
\paragraph{検証対応の統合．}
本節は V1--V10 全結果の横断統合点である．各実験が測定した式・離散化・物理指標の対応を
以下に集約する：
\begin{itemize}\setlength{\itemsep}{0pt}
  \item V1 (\autoref{sec:single_phase_ns}) — Navier--Stokes \eqref{eq:NS_onefluid}
        と AB2 / IMEX-BDF2（\autoref{ch:time_integration}）に対するエネルギー減衰と非圧縮残差．
  \item V2 (\autoref{sec:single_phase_ns}, \autoref{sec:kovasznay}) —
        CCD 6 次空間（\autoref{sec:ccd_def} 式~\eqref{eq:CCD_I}）に対する Kovasznay 残差．
  \item V3, V5 (\autoref{sec:bf_static_droplet}, \autoref{sec:spurious_multistep})
        — BF 力学的バランス \eqref{eq:bf_fccd_unification} + CSF
        \eqref{eq:csf_final} に対する Laplace 圧維持と寄生流れ．
  \item V4 (\autoref{sec:galilean_offset}) —
        固定壁系の Galilean offset 残差に対する参照系シフト後の速度場差．
  \item V6, V7 (\autoref{sec:density_ratio_sweep},
        \autoref{sec:imex_bdf2_twophase_time}) — 分相 PPE \eqref{eq:split_ppe} +
        DC + HFE に対する密度比安定性と二相時間精度．
  \item V8, V9, V10 (\autoref{sec:nonuniform_grid_ns}) — 非一様 NS + local
        $\varepsilon$ + CLS 移流に対する静止液滴，局所 $\varepsilon$ 比較，体積保存性．
\end{itemize}
本節 \autoref{tab:v_accuracy_summary} はこの全対応の精度結果を一表に集約する
唯一の総合表であり，他章 (\autoref{sec:conclusion}) からの参照は
本表をエンドポイントとする．
