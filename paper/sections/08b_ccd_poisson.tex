% paper/sections/08b_ccd_poisson.tex
%
% §8 圧力 Poisson 方程式の続き（08_pressure.tex からの分割）
% 内容：CCD-Poisson 演算子の行列構造・変密度拡張・Balanced-Force整合性・手法比較・単体検証
%
%% ═══════════════════════════════════════════════════════════════════════════════
\subsection{CCD-Poisson 演算子の行列構造と変密度拡張}
\label{sec:ccd_poisson_matrix}
%% ═══════════════════════════════════════════════════════════════════════════════

§~\ref{sec:projection_derivation} で得た PPE（式~\eqref{eq:PPE_varrho}）を，
CCD 法（第~\ref{sec:CCD}章）を用いて $\Ord{h^6}$ 精度で離散化する．
本節では2つの課題を同時に解決する：

\begin{enumerate}
  \item \textbf{$\Ord{h^2} \to \Ord{h^6}$ への精度向上：}
        圧力 $p$，1次導関数 $p'$，2次導関数 $p''$ を連立未知量として同時求解する
        CCD ブロック系（付録~\ref{app:ccd_elliptic}）により，
        速度補正の圧力勾配を $\Ord{h^6}$ 精度で評価する．
  \item \textbf{チェッカーボード（奇偶デカップリング）の抑制：}
        コロケート配置では格子スケール振動が生じる．
        本稿では Rhie--Chow の代わりに DCCD 散逸機構（§~\ref{sec:dccd_decoupling}，$\varepsilon_d=1/4$）を用い，
        DCCD フィルタ済み発散（式~\eqref{eq:dccd_ppe_rhs}）を PPE 右辺として構成する．
\end{enumerate}

行列の代数的導出（$\mathbf{L}_{\mathrm{CCD}}^{(x)} = \mathbf{E}_2\,\mathcal{M}_{\mathrm{CCD}}^{-1}\,\mathcal{R}$，
スペクトル半径 $\approx 3.43/h^2$）は付録~\ref{app:ccd_elliptic}，
Neumann 境界条件の組み込み手順は付録~\ref{sec:ccd_neumann_bc} を参照のこと．

\subsubsection{2次元変密度 CCD-Poisson 演算子の完全定式化}
\label{sec:ccd_2d_full}

2次元変密度 PPE $\bnabla\cdot(1/\rho\,\bnabla p) = q$ の CCD 離散化を示す．
$x$ 方向の演算子（式~\eqref{eq:L_split}）を格子点 $(i,j)$ で評価すると：
\begin{align}
  \bigl(\mathcal{L}_{\mathrm{CCD}}^{\rho,x}\bm{p}\bigr)_{i,j}
  &= \frac{1}{\rho_{i,j}}\bigl(D_x^{(2)} p\bigr)_{i,j}
  - \frac{(D_x^{(1)}\rho)_{i,j}}{\rho_{i,j}^2}\bigl(D_x^{(1)} p\bigr)_{i,j}
  \label{eq:Lx_varrho_discrete}
\end{align}
$y$ 方向も同様に：
\begin{align}
  \bigl(\mathcal{L}_{\mathrm{CCD}}^{\rho,y}\bm{p}\bigr)_{i,j}
  &= \frac{1}{\rho_{i,j}}\bigl(D_y^{(2)} p\bigr)_{i,j}
  - \frac{(D_y^{(1)}\rho)_{i,j}}{\rho_{i,j}^2}\bigl(D_y^{(1)} p\bigr)_{i,j}
  \label{eq:Ly_varrho_discrete}
\end{align}

合計演算子：
\begin{equation}
  \bigl(\mathcal{L}_{\mathrm{CCD}}^{\rho}\bm{p}\bigr)_{i,j}
  = \bigl(\mathcal{L}_{\mathrm{CCD}}^{\rho,x}\bm{p}\bigr)_{i,j}
  + \bigl(\mathcal{L}_{\mathrm{CCD}}^{\rho,y}\bm{p}\bigr)_{i,j}
  \label{eq:L_CCD_2d_full}
\end{equation}

\noindent\textbf{大域行列形式（スパース直接法）：}
式~\eqref{eq:L_CCD_2d_full} の点ごと演算子は，クロネッカー積スパース行列
$\mathbf{L}_{\mathrm{CCD}}^{\rho}$（式~\eqref{eq:L_CCD_2d_kron}）として陽に組み立て，
スパース LU 法で直接求解できる（付録~\ref{app:ccd_kronecker}，\ref{app:ccd_lu_direct}）．
Matrix-Free 評価手順（各反復で Thomas 法を $x$・$y$ 方向に交互適用，$O(N_x N_y)$）は
付録~\ref{sec:ccd_matrix_free_eval} を参照のこと．

\subsubsection{CSF + 最近接点 Hermite 延長による界面処理と寄生流れの抑制}
\label{sec:ccd_balanced_force}

CSF 体積力モデルでは，表面張力項 $\sigma\kappa\bnabla\psi$ と PPE 左辺の離散化を
整合させる「Balanced-Force 条件」が必要であり，CSF 近似誤差 $\Ord{\varepsilon^2} \approx \Ord{h^2}$
（$\varepsilon \sim h$）が寄生流れの物理的下限として残留する（詳細は付録~\ref{app:csf_balanced_force} 参照）．

本稿では CSF 体積力（§~\ref{sec:csf}）を予測子ステップに維持し，Balanced-Force 条件（§~\ref{sec:balanced_force}）
により圧力勾配と表面張力の離散化を整合させる：
Young--Laplace ジャンプ $[p]_\Gamma = \kappa/We$ に対して，CCD 演算子 $D^{(1)}$ による
圧力勾配と表面張力体積力 $(\kappa/We)D^{(1)}\psi$ に同一演算子を使用する
（式~\eqref{eq:balanced_pressure}，\eqref{eq:balanced_surface}）．
最近接点 Hermite 補間（§~\ref{sec:extension_pde}）が PPE 求解前に圧力場を界面越しに滑らかに延長し，
CCD ステンシル $(i-2,\ldots,i+2)$ が界面直近まで $C^\infty$ に近い場を参照できるようにする．
これにより：
\begin{itemize}
  \item CCD の Gibbs 振動が除去され，界面近傍の圧力勾配精度が大幅に向上する．
  \item PPE 右辺は DCCD フィルタ済み発散（式~\eqref{eq:dccd_ppe_rhs}）のみから構成される
        （Rhie--Chow Balanced-Force 補正はPredictor 段で適用；§~\ref{sec:rc_balanced_force}）．
  \item CCD 演算子が $p$・$p'$・$p''$ を同一スキームで評価するため，
        圧力勾配の離散化整合性が $\Ord{h^6}$ 精度で自動的に保たれる．
\end{itemize}

\begin{tcolorbox}[resultbox, title={最近接点 Hermite 延長 + CCD による寄生流れ抑制（FVM + CSF との比較）}]
\begin{center}
\small
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{4.2cm}p{4.8cm}p{3.2cm}}
\hline
手法 & 寄生流れの主因 & 理論的下限 \\
\hline
FVM + CSF + Rhie-Chow & 数値離散化誤差 $+$ CSF 物理誤差 & $\Ord{h^2}$（CSF 律速） \\
CCD + CSF + Balanced-Force & 数値成分低減，CSF 誤差残留 & $\Ord{h^2}$（CSF 律速） \\
\textbf{CCD + CSF + 最近接点 Hermite 延長 + DCCD}（本稿） & Gibbs 振動を除去，数値成分 $\Ord{h^6}$ & $\Ord{h^2}$（CSF 律速）${}^{*}$ \\
\hline
\end{tabular}
\end{center}
${}^{*}$CSF モデル誤差 $\Ord{\varepsilon^2}\approx\Ord{h^2}$（$\varepsilon\sim h$）が全手法共通のボトルネックとして残る．
本稿の手法では\textbf{数値離散化成分}を $\Ord{h^6}$（Hermite 補間精度）まで低減するため，
CSF 律速となる粗い格子でも数値成分が寄生流れに上乗せされない点が従来法との差異である．

\smallskip
（表~\ref{tab:ccd_vs_fvm}，§~\ref{sec:balanced_force}，表~\ref{tab:accuracy_summary} も参照）

\smallskip\noindent\textbf{注（$\Ord{h^6}$ の根拠）：}
本稿では Aslam~(2004) の Extension PDE が提案する仮想時間前進を経由せず，CCD が返す Hermite データ
$(f, f', f'')$ を用いた最近接点5次補間（式~\eqref{eq:hermite5}）で
延長後場 $q_\mathrm{ext}$ を直接構成する．
補間誤差は $\Ord{h^6}$（式~\eqref{eq:hermite5_error}）であり，
後段の CCD 圧力勾配評価と同次精度が保たれる
（詳細は §~\ref{sec:extension_pde_disc}）．
\end{tcolorbox}

\subsubsection{空間離散化スキームの定量的比較（FVM vs CCD）}
\label{sec:ccd_vs_fvm}

表~\ref{tab:ccd_vs_fvm} は PPE の\textbf{空間離散化スキーム}（FVM vs CCD）を比較する．
判断軸は空間精度と寄生流れ品質であり，CCD 採用の根拠を示す
（CCD 系を与件とした\textbf{線形ソルバ}の選択は §~\ref{sec:dc_comparison} で独立に扱う）．

\begin{table}[ht]
\centering
\caption{PPE 空間離散化スキームの比較（$N \times N$ 格子，$h = 1/N$）}
\label{tab:ccd_vs_fvm}
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{lccc}
\hline
空間離散化 & 空間精度 & 寄生流れ ($N=64$) & 実装難易度 \\
\hline
FVM（2次中心差分） & $\Ord{h^2}$ & $O(10^{-2})$ & 低 \\
\textbf{CCD}（本稿） & $\boldsymbol{\Ord{h^6}}$ & $\bm{O(10^{-5})}$ & 中 \\
\hline
\end{tabular}

\smallskip
注：「寄生流れ」は静止液滴ベンチマーク（§~\ref{sec:bench_droplet}）での
$\|\bu_{\mathrm{para}}\|_\infty/(\sigma/\mu)$ の目安値．
線形ソルバの選択は §~\ref{sec:dc_comparison} を参照．
\end{table}

%% 本章のまとめ（精度バランス表＋トレードオフ）は §8.4 の後に配置するため
%% 08h_pressure_summary.tex に移動（CHK-087: R5 — 読者が全節を読んでから結論に到達できるよう）．