% paper/sections/13e_nonuniform_ns.tex
%
% V8 : 非一様 NS 静止液滴 (alpha sweep, N sweep)
% V9 : 局所 epsilon 比較 (§14 stack nominal vs local)
% V10: Zalesak / CLS 移流 (uniform)
% ═══════════════════════════════════════════════════════════════════════════════
\subsection{非一様格子 NS と CLS 移流}
\label{sec:nonuniform_grid_ns}

\subsubsection{V8：非一様 NS 静止液滴}
\label{sec:nonuniform_ns_static}

\paragraph{検証対象．}
非一様格子（界面付近細密; 引き伸ばしパラメータ $\alpha$）上で
静止液滴 BF 平衡が長時間維持されること（\autoref{sec:U3_nonuniform_suite}）．
$\alpha = 1$ で一様格子と一致し，$\alpha = 2$ で界面付近 $h_{\min} = 0.5\, h_{\mathrm{avg}}$．

\paragraph{設定．}
$L=1$, 半径 $r=0.25$，$\sigma=1$, $\mathrm{We}=10$,
$\rho_l=10$, $\rho_g=1$．
$\Delta p_{\mathrm{exact}} = \sigma/(r\,\mathrm{We}) = 0.4$．
$N \in \{48, 64, 96\}$, $\alpha \in \{1, 2\}$．
$\Delta t = 0.20\,h_{\min}$, $n_{\mathrm{step}} = 200$．

\paragraph{結果．}
\begin{table}[!ht]
\caption{V8：非一様 NS 静止液滴 ($n_{\mathrm{step}}=200$)．
$\alpha=2$ では界面細密化で $\Delta p_{\mathrm{rel}}$ がやや悪化（数値ジオメトリ補正の交互作用）．}
\label{tab:V8_nonuniform_static}
\centering\small
\begin{tabular}{r r r r r}
\toprule
$N$ & $\alpha$ & $h_{\min}$ & $\Delta p_{\mathrm{final}}$ & $u_\infty^{\mathrm{end}}$ \\
\midrule
48 & 1 & $2.08\times 10^{-2}$ & $0.395$ & $1.42\times 10^{-4}$ \\
48 & 2 & $1.52\times 10^{-2}$ & $0.380$ & $4.14\times 10^{-4}$ \\
64 & 1 & $1.56\times 10^{-2}$ & $0.395$ & $1.35\times 10^{-4}$ \\
64 & 2 & $1.12\times 10^{-2}$ & $0.385$ & $4.61\times 10^{-4}$ \\
96 & 1 & $1.04\times 10^{-2}$ & $0.395$ & $5.64\times 10^{-4}$ \\
96 & 2 & $7.26\times 10^{-3}$ & $0.389$ & $4.86\times 10^{-4}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.92\linewidth]{figures/ch13_v8_nonuniform_static.pdf}
\caption{V8：非一様 NS 静止液滴．
$\Delta p_{\mathrm{final}}(\alpha)$ は $\alpha=1$ で $\Delta p_{\mathrm{exact}} \pm 1.2\%$ に収束．
$\alpha=2$ では $1\!-\!5\%$ の偏差（界面細密化が CSF 曲率の数値ジャコビアンと相互作用）．}
\label{fig:V8_nonuniform_static}
\end{figure}

\paragraph{判定と制約条件．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item $\alpha = 1$（一様）で $\Delta p_{\mathrm{rel}} \le 1.2\%$ → V3 結果と一致\checkmark．
  \item $\alpha = 2$（非一様）で $\Delta p_{\mathrm{rel}} \le 5\%$ — 設計通りの安定動作だが，
        非一様格子上の Heaviside--$\delta$ プロファイル（U5）と CSF $\kappa$ 推定の
        Jacobian 補正が要求精度を増やす．
  \item \textbf{$\alpha=1$ で $N=48\!\to\!64\!\to\!96$ の $u_\infty^{\mathrm{end}}$ 非単調}
        ($1.42\!\to\!1.35\!\to\!5.64\times10^{-4}$，$N=96$ で約 $4\times$ 上昇)：
        $\Delta p_{\mathrm{final}}$ 自体は $0.395$ 一定で BF 平衡は維持される．
        $u_\infty$ の一過性増加は，V5 と同様に CCD 高次微分作用素による短波長 spurious mode
        の励起に整合する（$\Delta t = h/4$ 固定下で $N$ 細密化が短波長成分の
        累積時間を変える）．圧力スケールは保たれているため BF 検証目的では合格，
        ただし長時間 spurious 抑制には HFE/pressure-jump 経路（U6, V6/V7）が必要．
\end{itemize}
検証対応：\autoref{sec:U3_nonuniform_suite} の非一様格子定義に対し，
静止液滴の圧力誤差と終端速度を測定する．

\subsubsection{V9：§14 stack による局所 $\varepsilon$ vs nominal $\varepsilon$ 比較}
\label{sec:local_eps_validation}

\paragraph{検証対象．}
非一様格子上で Heaviside--$\delta$ 厚み $\varepsilon$ を
\textbf{(A) nominal} ($\alpha=1,\ \varepsilon=1.5h$),
\textbf{(B) nominal} ($\alpha=2,\ \varepsilon=1.5h$),
\textbf{(C) local} ($\alpha=2,\ \varepsilon_{ij}=1.5\max(h_{x,i},h_{y,j})$)
で比較する．本試験は旧 CCD/CSF 経路ではなく，
\textbf{§14 と同じ operator stack}（FCCD interface/pressure gradient,
UCCD6 convection, HFE curvature, pressure-jump phase-separated FCCD PPE,
face-flux projection）で再実行する．

\paragraph{設定．}
縮約静止液滴条件 ($r=0.25,\ \sigma=0.10,\ \rho_l/\rho_g=10$)．
$N\in\{24,32\}$, $n_{\mathrm{step}}=10$,
theory CFL multiplier $0.50$．圧力ジャンプ形式では返却される $p$ は
物理 Laplace 圧そのものではなく投影補正圧の性格を持つため，
主指標は $u_\infty^{\max}$ と体積 drift とし，
$|\Delta p_{\mathrm{corr}}|/(\sigma/R)$ は補助診断としてのみ示す．

\paragraph{結果．}
\begin{table}[!ht]
\caption{V9：§14 stack 上の局所 $\varepsilon$ 比較 ($n_{\mathrm{step}}=10$)．
\textbf{C (local)} は nominal $\varepsilon$ より寄生速度を小さく保つ．}
\label{tab:V9_local_eps}
\centering\small
\begin{tabular}{r l r r r r r}
\toprule
$N$ & 設定 & $\alpha$ & $\varepsilon$ & $u_\infty^{\max}$ &
$|\Delta p_{\mathrm{corr}}|/(\sigma/R)$ & $|\Delta V|/V_0$ \\
\midrule
24 & A & 1 & nominal & $7.15\times 10^{-5}$ & $0.14$ & $3.33\times 10^{-10}$ \\
24 & B & 2 & nominal & $5.11\times 10^{-5}$ & $0.11$ & $2.15\times 10^{-9}$ \\
24 & C & 2 & \textbf{local} & $\mathbf{2.00\times 10^{-5}}$ & $\mathbf{0.04}$ & $8.40\times 10^{-10}$ \\
\midrule
32 & A & 1 & nominal & $4.55\times 10^{-5}$ & $0.11$ & $4.16\times 10^{-11}$ \\
32 & B & 2 & nominal & $3.24\times 10^{-5}$ & $0.08$ & $1.40\times 10^{-10}$ \\
32 & C & 2 & \textbf{local} & $\mathbf{5.01\times 10^{-6}}$ & $\mathbf{0.01}$ & $2.16\times 10^{-11}$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.92\linewidth]{figures/ch13_v9_local_eps.pdf}
\caption{V9：§14 stack 上の局所 $\varepsilon$ vs nominal $\varepsilon$．
左は寄生速度履歴，右は補助診断としての投影補正圧 contrast．
FCCD/HFE/pressure-jump PPE と組にすると，local $\varepsilon$ は
旧 CCD/CSF 経路で見られた発散的な寄生流れを示さない．}
\label{fig:V9_local_eps}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.98\linewidth]{figures/ch13_v9_ch14_stack_field.pdf}
\caption{V9：§14 stack で得た $N=32$ の 2D 速度場 snapshot．
白線は $\psi=0.5$ 界面．A/B/C の全ケースで静止液滴形状は保持され，
\textbf{C (local)} は界面近傍の速度 hot spot を最も小さく抑える．}
\label{fig:V9_ch14_stack_field}
\end{figure}

\paragraph{判定と制約条件．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{Local $\varepsilon$ は §14 stack と同時に使う条件で採用可}\textcolor{blue}{($\checkmark$)}．
        $N=32$ では C/A の $u_\infty^{\max}$ 比は $0.11$，
        C/B 比は $0.15$ であり，nominal $\varepsilon$ より静かな界面になる．
  \item ただし本 V9 は縮約静止液滴 probe であり，§14 の水--空気級 material benchmark
        そのものではない．local $\varepsilon$ の採用は，
        HFE curvature と pressure-jump PPE が同時に有効な構成に限定する．
\end{itemize}
検証対応：\autoref{sec:U5_heaviside_delta} の Heaviside--$\delta$ 定義に対し，
nominal $\varepsilon$ と局所 $\varepsilon$ の寄生流れおよび 2D hot spot を比較する．

\subsubsection{V10：FCCD/TVD--RK3 による Zalesak / 単一渦 CLS 移流}
\label{sec:zalesak_cls_advection}

\paragraph{検証対象．}
古典的 Zalesak slotted disk advection 問題~\cite{Zalesak1979}と可逆単一渦 deformation 問題を
FCCD flux-form + TVD--RK3 の CLS 移流として解き，
\textbf{conservative CLS mass 保存}，\textbf{重心追跡精度}，
および\textbf{可逆変形後の形状復元性}を評価する．
\autoref{sec:cls_advection} 式~\eqref{eq:cls_advection} の advection 項に
一様格子上の高次保存形差分 (\autoref{sec:fccd_def}) を組み合わせた評価であり，
非一様格子の検証は V8/V9 に分離する．

\paragraph{設定．}
標準 Zalesak slotted disk: 半径 $0.15$, slot 幅 $0.05$, slot 深さ $0.25$,
中心 $(0.5, 0.75)$．背景流 $\bm u(\bm x) = (-2\pi(y-0.5), 2\pi(x-0.5))$
（剛体回転; 1 周期 = $1$ 時間単位）．
$N \in \{64, 128\}$, $\alpha=1$．
$\Delta t_{\mathrm{est}} = 0.25\,h_{\min}/\max|\bm u|$ からステップ数を決め，
最終時刻が 1 周期に一致するよう $\Delta t$ を調整して積分する．
単一渦は同じ FCCD/TVD--RK3 輸送に Ridge--Eikonal 再初期化と質量修正（10 step ごと）を組み合わせ，
$N=128$, $\alpha=1$, $T=8$ の界面追跡なし固定一様格子 LeVeque 型可逆 deformation cycle として実行する．図示は従来通り，
初期円盤・最大変形・反転後最終場の 3 点スナップショットに限定する．

\paragraph{結果．}
\begin{table}[!ht]
\caption{V10：FCCD/TVD--RK3 による Zalesak CLS 移流 1 周期．
$\alpha=1$ の一様格子上で sharp-slot 形状保持と conservative CLS mass drift を測る．}
\label{tab:V10_zalesak}
\centering\small
\begin{tabular}{r r r r r}
\toprule
$N$ & $\alpha$ & centroid err & mass drift & $V_T/V_0$ \\
\midrule
64  & 1 & $1.091\times 10^{-2}$ & $6.178\%$ & $1.0618$ \\
128 & 1 & $4.688\times 10^{-3}$ & $1.535\%$ & $1.0153$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!ht]
\caption{V10：単一渦 deformation reversal（$N=128$, $\alpha=1$）．
$T=8$ の強変形で速度場を反転させ，界面追跡なしの固定一様格子上で Ridge--Eikonal と質量修正を 10 step ごとに適用して
$t=T$ で初期円盤へ戻るかを測る．}
\label{tab:V10_single_vortex}
\centering\small
\begin{tabular}{r r r r r}
\toprule
$N$ & $\alpha$ & $L^1$ reversal error & mass drift & $V_T/V_0$ \\
\midrule
128 & 1 & $2.690\times 10^{-2}$ & $9.360\times 10^{-2}\%$ & $1.0009$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.92\linewidth]{figures/ch13_v10_zalesak.pdf}
\caption{V10：Zalesak 1 周期の定量指標．
$\alpha=1$ は $N=128$ で conservative CLS mass drift $1.535\%$，centroid err $4.688\times 10^{-3}$．
Zalesak の薄い slot は一様格子でも解像度律速であり，非一様格子での形状追跡は V8/V9 とは別の検証項目に分離する．}
\label{fig:V10_zalesak}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.92\linewidth]{figures/ch13_v10_zalesak_snapshot.pdf}
\caption{V10：Zalesak slotted disk の 2D スナップショット（$N=128$）．
初期形状，$\alpha=1$ の 1 周期後，および $|\psi(T)-\psi(0)|$ を並べる．
薄い slot と円盤輪郭の崩れ方を一様格子上で直接確認できる．}
\label{fig:V10_zalesak_snapshot}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.92\linewidth]{figures/ch13_v10_single_vortex.pdf}
\caption{V10：単一渦 deformation reversal の 2D スナップショット．
初期円盤，最大変形時（$t=T/2$），反転後最終場（$t=T$）を並べる．$T=8$ の標準 single-vortex
強変形では巻き込みは明瞭になり，界面追跡なしの固定一様格子上で Ridge--Eikonal と質量修正を 10 step ごとに適用すると
conservative CLS mass drift は $9.360\times10^{-2}\%$ に抑えられる．
$L^1$ reversal error は $2.690\times10^{-2}$ である．}
\label{fig:V10_single_vortex}
\end{figure}

\paragraph{判定と制約条件．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item $\alpha=1$ (一様格子)：$N=128$ で conservative CLS mass drift $1.535\%$，
        centroid err $4.688\times10^{-3}$ → $\triangle$．
  \item 単一渦：$N=128,\alpha=1,T=8$ の界面追跡なし固定一様格子で渦巻き状の強変形は視覚的に確認できる．
        Ridge--Eikonal と質量修正（10 step ごと）で conservative CLS mass drift は $9.360\times10^{-2}\%$，
        $L^1$ reversal error は $2.690\times10^{-2}$ であり，保存性・形状復元性はいずれも条件付き（$\triangle$）．
  \item \textbf{非一様格子の扱い}：
        本節の Zalesak は $\alpha=1$ の sharp-slot 移流診断に固定する．
        非一様格子サポート ($\alpha>1$) は
        \textbf{NS 静止問題} (\autoref{sec:U3_nonuniform_suite}, V8 $\alpha=2$ で $\Delta p$ 誤差 $\le 5\%$)
        と local $\varepsilon$ 検証（V9）で扱う．
\end{itemize}
検証対応：式~\eqref{eq:cls_advection}，\autoref{sec:cls_advection} の CLS 移流，
\autoref{sec:fccd_def} の face value/grad に対し，体積 drift，重心誤差，
および単一渦 reversal error を測定する．
