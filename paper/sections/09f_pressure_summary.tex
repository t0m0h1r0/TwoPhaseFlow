% paper/sections/09f_pressure_summary.tex
%
% §9 章末まとめ：精度バランスと設計上のトレードオフ
% ※ 旧所在: 09_ccd_poisson.tex 内 → §9 章末に移動
%   理由：HFE（§9.4）・BC（§9.6）を全て読んだ後に精度全体像を示す構成に変更
%% ═══════════════════════════════════════════════════════════════════════════════
\subsection{本章のまとめ：精度バランスと設計上のトレードオフ}
\label{sec:pressure_accuracy_summary}
%% ═══════════════════════════════════════════════════════════════════════════════

変密度 PPE の導出（§~\ref{sec:projection_derivation}）は第~\ref{sec:collocate}章で行った．
本章では，CCD による $\Ord{h^6}$ 離散化（§~\ref{sec:ccd_poisson_matrix}），
分相 PPE 解法（§~\ref{sec:split_ppe_framework}），
HFE による界面越し場延長（§~\ref{sec:field_extension}），
各相内の欠陥補正法による求解（§~\ref{sec:defect_correction_main}），
境界条件と数値安定性（§~\ref{sec:ppe_bc_stability}）を論じた．
以下に，各コンポーネントの精度を整理する．

\begin{table}[htbp]
\centering
\caption{数値手法コンポーネント別の精度まとめ}
\label{tab:accuracy_summary}
\renewcommand{\arraystretch}{1.3}
\footnotesize
\begin{tabular}{lp{0.28\linewidth}cc}
\toprule
コンポーネント & 手法 & 空間精度 & 時間精度 \\
\midrule
CLS 移流 & DCCD + TVD-RK3 & CCD $\Ord{h^6}$；filter $\Ord{\varepsilon_d h^2}$§ & $\Ord{\Delta t^3}$ \\
幾何量計算（曲率等） & CCD 法 & $\Ord{h^6}$ & — \\
PPE (Lv.1：SSPRK3 検証) & 一括 PPE + DC + CCD（等密度） & $\Ord{h^6}$‡ & — \\
PPE (Lv.2：プロダクション) & 一括 PPE + GFM 補正 + DC & $\Ord{h^4}$‡‡ & — \\
PPE (Lv.3：純 FCCD DNS) & 分相 PPE + DC $k{=}3$ + CCD 勾配 & $\Ord{h^5}$/$\Ord{h^6}$‡‡‡ & — \\
NS 粘性項 & CCD + CN（交差項陽的）${}^{\P}$ & $\Ord{h^2}$/$\Ord{h^6}$${}^{\P}$ & $\Ord{\Delta t^2}$ \\
NS 対流項（Predictor） & CCD（1次微分）+ AB2 外挿 & $\Ord{h^6}$ & $\Ord{\Delta t^2}$ \\
Projection スプリッティング & IPC 法（van Kan 1986） & — & $\Ord{\Delta t^2}$ \\
HFE（分相 PPE 基盤，§~\ref{sec:field_extension}） & 最近接点 Hermite 補間 & 1D / axis-aligned：$\Ord{h^6}$；2D 一般：完全テンソル版に限り $\Ord{h^6}$$\star$ & — \\
\midrule
\textbf{全体（空間律速：CSF モデル誤差）} & & $\boldsymbol{\Ord{h^2}}$† & — \\
\textbf{全体（時間律速：均質流域）} & & — & $\boldsymbol{\Ord{\Delta t^2}}$ \\
\bottomrule
\end{tabular}

\smallskip
\noindent†\ CSF 正則化 $\delta_\varepsilon$（$\varepsilon = \Ord{h}$）によるモデル誤差 $\Ord{\varepsilon^2} = \Ord{h^2}$ が
全体の空間精度を律速する．\\
\noindent‡\ Lv.1 等密度均一格子・周期 BC の理想化では $\Ord{h^6}$（§\ref{sec:dc_accuracy}）．\\
\noindent‡‡\ Lv.2 は一括 smoothed Heaviside PPE + GFM 補正；界面 $\Ord{h^2}$ が律速し DC で $\Ord{h^4}$ に昇格．\\
\noindent‡‡‡\ Lv.3 分相 PPE + DC $k{\geq}3$ では各相内 $\Ord{h^7}$（Dirichlet 超収束）/ $\Ord{h^5}$（Neumann BC 律速）を密度比に直接依存しない形で達成（§~\ref{sec:verify_split_ppe_dc}）；速度補正の勾配は CCD $\Ord{h^6}$．\\
\noindent§\ CCD 微分の局所切断誤差は $\Ord{h^6}$ だが，
固定強度 DCCD フィルタは滑らかなモードに $\Ord{\varepsilon_d h^2}$ の修正を加える．
質量積分誤差や形状誤差は別指標であり，この局所次数と混同しない．\\
\noindent¶\ ADI 陰的スウィープの対角係数には2次 FD（$\Ord{h^2}$）を使用；交差微分項は CCD $\Ord{h^6}$ で陽的に評価（§~\ref{sec:algorithm}）．
$Re \gg 1$ では粘性項の寄与 $\sim \Ord{h^2/Re}$ が CSF モデル誤差 $\Ord{h^2}$ と同次以下に収まるため実効精度への影響は小さい．\\
\noindent$\star$\ 混合微分 $q_{xy},q_{xxy},q_{xyy},q_{xxyy}$ を必要とする完全テンソル版のみ
2D 斜め界面で $\Ord{h^6}$ 保証；混合微分を省略した簡略版は $\Ord{h^4}$ 以下に律速される
（§\ref{sec:extension_pde_disc}）．
\end{table}

\begin{tcolorbox}[warnbox, title={精度ミスマッチと設計上のトレードオフ}]
\begin{enumerate}
  \item \textbf{空間精度：}
        全数値コンポーネントが $\Ord{h^5}$--$\Ord{h^6}$ に揃っている（平滑領域）．
        全体精度の律速は CSF 正則化 $\delta_\varepsilon$ による $\Ord{h^2}$ モデル誤差である．
        分相 PPE + DC $k{=}3$ は各相内 $\Ord{h^5}$（Neumann BC 律速）を密度比に直接依存しない形で達成し，
        HFE（§~\ref{sec:field_extension}）はジャンプ補正済み片側データを延長し，
        CCD ステンシルが不連続値を跨がないようにする．

  \item \textbf{時間精度：}
        均質流域では AB2 + IPC により全体 $\Ord{\Delta t^2}$（§~\ref{sec:ipc_derivation}）．
        変粘性界面で交差粘性項を陽的に扱う場合，または表面張力卓越時に
        RC 補正の時刻ずれが支配する場合は $\Ord{\Delta t}$ 成分が残る．
\end{enumerate}
\end{tcolorbox}

\begin{tcolorbox}[mybox, title={設計選択の根拠：分相 PPE + DC + CCD 勾配の統合設計}]
\label{box:ppe_design_rationale}
本稿の PPE 求解戦略は\textbf{分相 PPE 解法}を主軸とし，以下の3つの原則から帰結する．

\begin{enumerate}
  \item \textbf{分相 PPE による密度比非依存化}．
        各相で定数密度の PPE を解き，界面では圧力ジャンプと法線フラックス条件を課す
        （§~\ref{sec:split_ppe_framework}）．
        HFE（§~\ref{sec:field_extension}）は，そのジャンプ補正済み片側データを界面越しに延長する．
        界面を横切るステンシルが排除されるため，
        DC $k{=}3$ は各相内で $\Ord{h^7}$（Dirichlet）/ $\Ord{h^5}$（Neumann BC 律速）を
        密度比に直接依存しない形で達成する
        （§~\ref{sec:verify_split_ppe_dc}）．

  \item \textbf{Balanced-Force 整合性：同一演算子の強制}．
        Balanced-Force 条件は，圧力勾配 $\bnabla p^{n+1}$ と表面張力 $\sigma\kappa\bnabla\psi$ に
        \textbf{同一の離散微分演算子}を使用することで，演算子不一致に由来する寄生流れ成分を抑える
        （§~\ref{sec:rc_balanced_force}）．
        速度補正 $\delta\bu = -\Delta t\,\bnabla p^{n+1}/\rho$ の勾配に
        CCD $\Ord{h^6}$（均一格子）または
        face-average 勾配 $\mathcal{G}^\text{adj}$（非一様格子 + 壁面 BC；
        §~\ref{sec:fvm_ccd_corrector}）を使い，
        表面張力項 $\kappa\bnabla\psi$ にも同一の CCD 演算子を適用することで，
        この整合性を高次に維持する．

  \item \textbf{疎行列直接法による Step~2 解法誤差の抑制}．
        DC 法の補正方程式 $L_L$（§~\ref{sec:dc_sweep}）を
        ゲージ固定後の 2D 疎行列 LU などで解くことで，
        Step~2 の線形解法残差は機械精度近くまで下げられる．
        方向分離スウィープを用いる場合は近似前処理として扱い，
        直接法ケースとは残差保証を分ける（§~\ref{sec:defect_correction_main}参照）．
\end{enumerate}

\noindent\textbf{全体精度の律速：}
CSF 正則化 $\delta_\varepsilon$（$\varepsilon=\Ord{h}$）によるモデル誤差
$\Ord{\varepsilon^2}=\Ord{h^2}$ が全体の空間精度を律速する（表~\ref{tab:accuracy_summary}†）．
分相 PPE + DC $k{=}3$ の $\Ord{h^5}$ は CSF 誤差を十分に上回り，
PPE ソルバが精度ボトルネックとならないことを保証する．
\end{tcolorbox}

\noindent\textbf{GPU-native FVM 実装バリアント（付録~\ref{app:gpu_fvm}）}：
本章は PPE の\textbf{数学的定式化と精度}を扱う．
同一の面ローカル代数を GPU 上で行列フリーに実装する経路
（演算子カリキュラス $L_\text{FVM}(\rho)p = \sum_a D_a A_a(\rho) G_a p$
は付録~\ref{app:gpu_fvm_ops}，可変バッチ PCR は付録~\ref{app:gpu_fvm_pcr}，
D2H/H2D 規律は付録~\ref{app:gpu_fvm_d2h}）は
\textbf{正確性を変えずに性能を変える}補助設計であり，
付録~\ref{app:gpu_fvm} に詳述する．
Level 2 プロダクションでは本文の分相 FCCD PPE 経路と
付録の GPU-native FVM 経路をワークロードに応じて切り替える
二路選択が実用的である．
