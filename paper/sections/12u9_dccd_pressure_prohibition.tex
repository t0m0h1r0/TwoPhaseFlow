% paper/sections/12u9_dccd_pressure_prohibition.tex
%
% U9: DCCD-on-pressure prohibition (NEGATION test) — Tier VI
% ═══════════════════════════════════════════════════════════════════════════════
\subsubsection{U9：DCCD on pressure 禁止 [Tier VI]}
\label{sec:U9_dccd_pressure_prohibition}

\paragraph{検証対象（否定検証）．}
第~\ref{sec:balanced_force}章 で確立した設計規則：
「\textbf{DCCD（3 点フィルタ）は圧力場 $p$ に適用してはならない}」
（§\ref{sec:bf_p2_adjoint} 末尾）の根拠を，
DCCD 適用前後の $\rho^{-1}\nabla p$ の差を定量化することで
\textbf{否定検証する}．これは PR-5 Algorithm Fidelity に基づく
\textbf{negation test}（規則違反時に何が起こるかを示す）である．

\paragraph{設定．}
2D MMS 圧力場 $p^\star = \sin(2\pi x)\sin(2\pi y)$．
$N \in \{32, 64, 128, 256\}$，DCCD パラメータ $\eps_d \in \{0.05, 0.25\}$．
\begin{itemize}\setlength{\itemsep}{0pt}
  \item ベースライン：CCD（フィルタ無）で $\nabla^2 p$ を計算．
        $L_\infty^{\mathrm{unfiltered}} = \|\nabla^2 p_{\mathrm{CCD}} - \nabla^2 p^\star\|_\infty$．
  \item 違反試行：DCCD（$\eps_d > 0$）で圧力を先に平滑化し，その後 $\nabla^2 \tilde p$ を計算．
        $L_\infty^{\mathrm{diff}} = \|\nabla^2 p_{\mathrm{CCD}} - \nabla^2 \tilde p_{\mathrm{CCD}}\|_\infty$．
  \item ratio $\mathcal{R} = L_\infty^{\mathrm{diff}} / L_\infty^{\mathrm{unfiltered}}$ の
        $h \to 0$ 挙動を観察．
\end{itemize}

\paragraph{結果．}

\begin{table}[!ht]
\caption{U9: DCCD on pressure による $\nabla p$ 摂動．
$L^{\mathrm{unfiltered}}_\infty$（CCD ベースライン）は $\Ord{h^6}$ で機械精度床へ向かう一方，
$L^{\mathrm{diff}}_\infty$（DCCD 摂動）は両 $\eps_d$ で $\Ord{h^2}$ にしか減衰せず，
ratio $\mathcal{R} = L^{\mathrm{diff}}_\infty / L^{\mathrm{unfilt}}_\infty$ は
\textbf{$h$ 細分化で $\Ord{h^{-4}}$ で巨大化し，CCD baseline の round-off 床に達する $N\!=\!128$ 近傍で頭打ち}．
$N\!=\!256$ で R が反転して減少するのは baseline 誤差が $\Ord{h^6}$ から
丸め誤差床（$\sim\!10^{-10}$）に切替わるため．
$\eps_d = 0.25$ で最悪 $\mathcal{R} \approx 3.3 \times 10^{8}$ ($N\!=\!128$)．}
\label{tab:U9_dccd_violation}
\centering\small
\begin{tabular}{r r c c c c c}
\toprule
$N$ & $h$ & $L^{\mathrm{unfilt}}_\infty$ & $L^{\mathrm{diff}}_\infty\,(\eps_d\!=\!0.05)$ & $L^{\mathrm{diff}}_\infty\,(\eps_d\!=\!0.25)$ & $\mathcal{R}\,(\eps_d\!=\!0.05)$ & $\mathcal{R}\,(\eps_d\!=\!0.25)$ \\
\midrule
$32$  & $0.0313$ & $1.14\times 10^{-6}$  & $0.303$  & $1.510$  & $2.66\times 10^{5}$ & $1.32\times 10^{6}$ \\
$64$  & $0.0156$ & $1.78\times 10^{-8}$  & $0.0760$ & $0.380$  & $4.27\times 10^{6}$ & $2.14\times 10^{7}$ \\
$128$ & $0.0078$ & $2.90\times 10^{-10}$ & $0.0190$ & $0.0951$ & $6.56\times 10^{7}$ & $3.28\times 10^{8}$ \\
$256$ & $0.0039$ & $1.30\times 10^{-10}\,^\dagger$ & $0.00476$ & $0.0238$ & $3.67\times 10^{7}$ & $1.84\times 10^{8}$ \\
\bottomrule
\end{tabular}
\\[2pt]
{\footnotesize $\dagger$: $N\!=\!256$ で $L^{\mathrm{unfilt}}_\infty$ が round-off 床に達し
（$\Ord{h^6}$ 減衰が停止），ratio $\mathcal{R}$ も飽和．これは $\eps_d > 0$ の DCCD 違反量
が真に $\Ord{h^2}$ で減衰するのに対し，CCD baseline は $\Ord{h^6}$ → 機械精度床へ早期飽和する
ためで，$\mathcal{R}$ の絶対値ではなく \textbf{$L^{\mathrm{diff}}_\infty$ vs $L^{\mathrm{unfilt}}_\infty$ の漸近次数差}（4 次以上）が違反指標．}
\end{table}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.88\linewidth]{figures/ch12_u9_dccd_pressure_prohibition}
\caption{U9：DCCD on pressure 禁止規則の根拠（否定検証）．
ベースライン CCD は $\Ord{h^6}$ で機械精度床へ収束する一方，
DCCD 適用時は $L_\infty^{\mathrm{diff}}$ が $\Ord{h^2}$ にしか減衰せず ratio $\mathcal{R} = L^{\mathrm{diff}}_\infty / L^{\mathrm{unfilt}}_\infty$ が $\Ord{h^{-4}}$ 増幅（固定 $\eps_d$ で $N=32\!\to\!128$ が約 2.4 decade，$\eps_d$ 横断を含む全体は $10^5\!\to\!3\!\times\!10^8$ の 3.5 decade）に達する．
$\nabla p$ には DCCD 不可・CCD 厳守の規則を定量的に裏付ける．
}
\label{fig:ch12_u9}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.98\linewidth]{figures/ch12_u9_dccd_prohibition_field.pdf}
\caption{U9 prohibition map（$N=256$，共有カラースケール）．
$|\nabla^2 p - \nabla^2 \tilde p|$ を $\eps_d \in \{0.05, 0.25\}$ で 2D 表示する．
左：$\eps_d=0.05$（最大 $\sim\!4.8\times 10^{-3}$），
右：$\eps_d=0.25$（最大 $\sim\!2.4\times 10^{-2}$）．
両 panel は同一 colormap・同一 vmax を共有し，$\eps_d$ を 5 倍にすると違反量も
ほぼ 5 倍に増えること（線形依存）と，違反位置が
$p^\star = \sin(2\pi x)\sin(2\pi y)$ の高周波成分が乗る対角と反対角の
鞍点ベルト（$y\!=\!x$, $y\!=\!1-x$）に集中することを直接視覚化する．
表~\ref{tab:U9_dccd_violation} の 0D 数値（$\eps_d=0.25$ で $\sim\!2.4\times 10^{-2}$）が，
DCCD 摂動を圧力場に当てると面状ではなく
特定 stencil パターンに集中して破壊的に効くことを示している．
\textbf{この空間構造こそが BF--coupling の整合性破壊の幾何学的根拠}である．}
\label{fig:ch12_u9_field}
\end{figure}

\paragraph{判定と次章への接続．}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item ratio $\mathcal{R}$ は $N\!=\!32 \to 128$ で $\Ord{h^{-4}}$ 増幅
        （固定 $\eps_d$ で約 2.4 decade，$\eps_d$ 横断幅含めて
        $10^5 \to 3 \times 10^8$ の 3.5 decade）．$N\!=\!256$ で減少に転じるのは
        CCD baseline ($L^{\mathrm{unfilt}}_\infty$) が機械精度床に達して $\Ord{h^6}$
        減衰を停止するためで，DCCD 違反量自身（$L^{\mathrm{diff}}_\infty$）は
        $\Ord{h^2}$ で減衰し続ける．これは DCCD が圧力空間では「フィルタ」ではなく
        \textbf{破壊的摂動}として作用し，baseline と漸近 4 次の次数差を作ることを意味する．
  \item BF--coupling（§\ref{sec:bf_p2_adjoint}）の整合性条件は
        $\nabla p$ と $\sigma\kappa\hat{n}$ の \textbf{離散的同等表現}を要求する．
        DCCD は移流項に最適化された 3 点フィルタであり，圧力勾配の整合性を
        $\Ord{1}$ で破壊する．
  \item $\eps_d \to 0$ での連続極限は CCD と一致するが，$\eps_d > 0$ では
        $h$ 細分化で逆に違反量が増大する．これは DCCD の 周波数応答
        $H(\xi; \eps_d) = 1 - 4\eps_d \sin^2(\xi/2)$
        （式~\eqref{eq:dccd_filter_transfer}; §\ref{sec:dccd_derivation}）が
        $\xi=\pi$ で $1-4\eps_d$ まで減衰する高波数寄与を持つためである．
  \item \textbf{結論}：本実験は規則「DCCD $\not\subset$ pressure」の
        定量的根拠を与える．移流項 ($\nabla \cdot (\mathbf{u}\mathbf{u})$) には DCCD 適用，
        圧力勾配 ($\nabla p$) には CCD 厳守，が algorithm fidelity（PR-5）の
        必須条件である．\checkmark（negation 成立）．
\end{itemize}
検証対応：
式~\eqref{eq:dccd_filter_transfer}（DCCD 周波数応答）と
§\ref{sec:bf_p2_adjoint} の圧力勾配整合条件に対し，
DCCD を圧力へ適用したときの差分誤差と増幅比を測定する．
