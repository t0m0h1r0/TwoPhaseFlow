% paper/sections/10d_ridge_eikonal_nonuniform.tex
%
% §10.5  Ridge–Eikonal の非一様格子定式化
%        §3.4 で導入した一様格子上の Ridge–Eikonal 再初期化を
%        非一様格子へ拡張する 4 補正 D1–D4 を本節で定式化する．
%
% ═══════════════════════════════════════════════════════════════════════════════
\subsection{Ridge--Eikonal の非一様格子定式化：D1--D4 補正}
\label{sec:ridge_eikonal_nu_grid}
% ═══════════════════════════════════════════════════════════════════════════════

\S\ref{sec:ridge_eikonal} で構築した一様格子上の Ridge--Eikonal 再初期化を
界面適合非一様格子（密度因子 $\alpha>1$）へ拡張すると，
(i) Gaussian 半幅 $\sigma$ の物理セル分解能が不均一化，
(ii) $\xiidx$ 空間で Hessian を評価すると実用的困難性に抵触，
(iii) FMM~\cite{Sethian1996} の二次式更新が単位ステップを仮定して $|\bnabla_x\phi|=1$ を破る，
(iv) 壁面接触界面では Gaussian support と FMM seed 集合が
閉領域 $\overline{\Omega}$ 上で閉じていない，
の 4 か所で一様格子不変量が破れる．これらを 4 補正
D1（$\sigma_\text{eff}$ 空間スケーリング）・D2（物理空間 Hessian）・
D3（非一様 FMM）・D4（$\varepsilon$-widening の空間依存化）と
壁閉包補正 W で解消し，
\S\ref{sec:grid_density}（グリッド密度関数）・
\S\ref{sec:grid_coord}（メトリクス係数）・
\S\ref{sec:ccd_metric}（非一様 CCD）の幾何量へ数学的に接続する．
本節は「各補正がどの格子量に依存し，
どの離散演算子と整合するか」を明示することを目的とする．

\begin{remark}[本節の記号規約]
\label{rem:ridge_nu_jx_convention}
\S\ref{sec:grid_coord} は主本文メトリクス係数を
$J_x = \mathrm{d}\xi/\mathrm{d}x$（ヤコビアン）として定義したが，
本節ではセル幅計算および FMM 閉形式解との整合のため，
\textbf{逆数の幾何量}
$\mathsf{X}_\xi(\xi) \equiv \mathrm{d}x/\mathrm{d}\xi = 1/J_x^{\mathrm{main}}$ を用いる．
以降\textbf{本節では一貫して $\mathsf{X}_\xi$ 記号}を使用し，
$J_x$ 記号は登場しない．主本文の $J_x$ への
変換は $J_x^{\mathrm{main}} = 1/\mathsf{X}_\xi$ で読み替える．
\end{remark}

% -------------------------------------------------------------------------------
\subsubsection{D1--D4 の数学的定義}
\label{sec:ridge_nu_d1d4_math}
% -------------------------------------------------------------------------------

格子密度因子 $\alpha>1$ の界面適合格子
（§~\ref{sec:grid_density}）では，§~\ref{sec:ridge_aux_field}--§~\ref{sec:ridge_fmm_reconstruction}
の導出で暗黙に仮定された一様格子不変量が 4 か所で破れる：
(i) Gaussian 半幅 $\sigma$ の物理セル分解能が不均一化，
(ii) $\xiidx$ 空間で Hessian を評価すると
後述の実用的困難性（注記~\ref{rem:xi_hessian_forbidden}）に抵触，
(iii) FMM の二次式更新が単位ステップを仮定して $|\bnabla_x\phi|=1$ を破る，
(iv) 壁面接触界面では自由空間 Gaussian と内部点専用 seed が
閉包上の零集合を保存しない．
以下 4 つの補正 D1--D4 と壁閉包 W でこれらを解消する．

\paragraph{D1 — $\sigma_\text{eff}(\bm{x})$ の空間スケーリング}
基準格子幅を格子点数 $N_a$ に対して
$h_\text{ref}=(\prod_{a} L_a/(N_a-1))^{1/d}$，
局所幾何平均を $h(\bm{x})=\sqrt{h_x(i)\,h_y(j)}$ とし，
\begin{equation}
  \sigma_\text{eff}(\bm{x}) = \sigma_0 \cdot \frac{h(\bm{x})}{h_\text{ref}},
  \qquad \sigma_0 \in \mathbb{R}_{>0}\text{（}h_\text{ref}\text{ 単位）}
  \label{eq:sigma_eff}
\end{equation}
と修正する．
物理セル単位では $\sigma_\text{eff}(\bm{x})/h(\bm{x}) = \sigma_0/h_\text{ref}$ が定数となり
一様格子での設計値をそのまま $\sigma_0$ に引き継げる．

\paragraph{D2 — 物理空間 Hessian（$\xi$ 空間 Hessian 禁止定理）}
$\xiidx$ 空間で Hessian を組み立て
chain rule $\bnabla_x^2 = J^{\!\top}\bnabla_\xi^2 J
+ (\bnabla_\xi J^{\!\top})\bnabla_\xi$
で物理空間に変換する素朴な手順は禁じられる．

\begin{remark}[$\xi$ 空間 Hessian の実用的困難性]\label{rem:xi_hessian_forbidden}
界面遷移幅が $\le 4$ セルのとき，
$\xi$ 空間 Hessian を経由する任意の計算は $\mathcal{O}(10^{-1})$ の
フロア誤差を持つ（実験的根拠は §~\ref{sec:xi_sdf_reinit}）．
リッジ定義 \eqref{eq:ridge_set_def} は
$\sigma_\text{eff}\sim 3h$ の範囲で評価されるため，
この条件は一般に違反される．
\end{remark}

許容される評価経路：
\begin{enumerate}
  \item\textbf{Approach A（主経路）}——物理空間 Direct 非一様 CCD
        で Hessian $\mathbf{H}_{\xiridge}=\bnabla_x^2\xiridge$ を直接評価する．
  \item\textbf{Approach B（確認用 fallback）}——
        物理空間二階 FD で $H_{xx},H_{xy},H_{yy}$ を近似し，
        符号判定 $\lambda_{\min}(H)<0$ または
        $\hat{\bm{n}}^{\!\top}H\hat{\bm{n}}<0$ を
        $H^{\text{FD}}$ で行う．これは 2 次精度のデバッグ経路に限り，
        Morse 非退化点でも固有値の符号余裕が小さい場合は Approach A を用いる．
\end{enumerate}

\paragraph{D3 — 非一様 FMM}
標準 FMM の二次更新
$d_\text{new}=\tfrac{1}{2}(a_x+a_y+\sqrt{2-(a_x-a_y)^2})$
は $h_x=h_y=1$ を前提とする．
ここで $a_x, a_y$ は各方向で「Accepted」集合に属する近傍点（風上側）の $d$ 最小値である
（標準 FMM の upwind 選択規則；詳細は~\cite{Sethian1996}）．
物理空間 Eikonal
\begin{equation}
  \frac{(d-a_x)^2}{h_x^2}+\frac{(d-a_y)^2}{h_y^2}=1
  \label{eq:fmm_nonuniform}
\end{equation}
の閉形式解（大きい方の根）は
\begin{equation}
  d = \frac{a_x/h_x^2 + a_y/h_y^2 + \sqrt{D}}
           {1/h_x^2 + 1/h_y^2},\qquad
  D = \Bigl(\tfrac{1}{h_x^2}+\tfrac{1}{h_y^2}\Bigr)
      - \frac{(a_x-a_y)^2}{h_x^2 h_y^2}.
  \label{eq:fmm_closed_form}
\end{equation}
$D<0$ のときは一次元単調更新 $d=\min(a_x+h_x,\,a_y+h_y)$ を採用する．
粘性一意性は \eqref{eq:fmm_nonuniform} が物理空間で書かれている限り
§~\ref{sec:ridge_fmm_reconstruction} の粘性解一意性証明がそのまま成立する．

\paragraph{D4 — $\varepsilon$-widening の空間依存化}
再構成幅を
\begin{equation}
  \varepsilon_\text{local}(\bm{x})
  = \varepsilon_\text{scale}\cdot \varepsilon_\xi \cdot h(\bm{x}),
  \qquad \varepsilon_\xi = \varepsilon/h_\text{min}
  \label{eq:eps_local}
\end{equation}
と空間依存にする
（$h_\text{min} \equiv \min_{\bm{x}} h(\bm{x})$ は計算領域の最小格子幅；
非一様格子の定義は §~\ref{sec:grid}）．

\noindent
$\varepsilon_\text{scale}$ は「再構成された SDF 幅を
局所格子幅とどの程度整合させるか」を決める無次元因子であり，
一様格子の既定値を非一様格子へ移植する際にも
式~\eqref{eq:eps_local} の空間因子により自然に吸収される．
本稿では以降，代表値として $\varepsilon_\text{scale}=1.4$ を用いるが，
感度評価と補正条件は本節以降で扱う．

\paragraph{W — 壁閉包補正（wall-closure）}
壁面に接する界面は領域内部の多様体ではなく，閉領域上の
\begin{equation}
  \overline{\Gamma}(t)=\Gamma(t)\cup C(t),\qquad
  C(t)=\overline{\Gamma}(t)\cap\partial\Omega
  \label{eq:wall_closed_interface}
\end{equation}
として扱う．
中立濡れ・Neumann 型 CLS 壁条件では，壁法線方向の幾何拡張は偶関数鏡像であり，
Ridge 補助場は
\begin{equation}
  \xiridge^{W}(\bm{x})
  =
  \sum_{\bm{c}_k\in S_\Gamma}
  \sum_{R\in\mathcal{R}_W}
  \exp\!\left(-\frac{|\bm{x}-R\bm{c}_k|^2}{\sigma_\text{eff}(\bm{x})^2}\right)
  \label{eq:ridge_wall_mirror}
\end{equation}
で評価する．
ここで $\mathcal{R}_W$ は恒等写像と，Gaussian support band 内で関与する
壁鏡像（角では合成鏡像も含む）である．
これにより壁面で切断された半 Gaussian による ridge の内側バイアスを除く．

FMM の Dirichlet seed 集合も内部交差だけでは不十分である．
ただし seed は Eikonal 問題の境界条件そのものなので，
\textbf{実際の零集合または物理的に指定された接触線に限る}．
壁 trace 上の符号変化・厳密零点を含めて
\begin{equation}
  S_h
  =
  \mathcal{Z}_h(\phi_0;\Omega^\circ)
  \cup
  \mathcal{Z}_h(\phi_0;\partial\Omega_W)
  \cup
  S_{\mathrm{contact}}^{\mathrm{prescribed}},
  \qquad
  |\bnabla_x\phi|=1,\quad \phi|_{S_h}=0
  \label{eq:wall_seed_set}
\end{equation}
を解く．
ここで $\partial\Omega_W$ は wall 軸の物理境界であり，
周期軸の像平面は $\partial\Omega_W$ に含めない．
Gaussian ridge mask は幾何候補の検出・壁鏡像補正には使えるが，
$S_h$ に人工的な零 seed を追加する根拠にはならない．
人工 seed は Eikonal の境界集合を変更し，
固定トポロジー再初期化で保存すべき零点集合を動かすためである．
さらに質量補正は接触線 seed を動かしてはならないため，
\begin{equation}
  \phi \leftarrow \phi + \lambda\,\chi_{\mathrm{free}}(\bm{x}),
  \qquad
  \chi_{\mathrm{free}}=0\ \text{on a contact-line band}
  \label{eq:contact_pinned_mass_correction}
\end{equation}
とし，$\lambda$ は通常の体積制約から決める．
この補正は Navier--Stokes の壁面速度・圧力条件を変更するものではなく，
Ridge--Eikonal と格子 monitor が $\overline{\Omega}$ 上の界面閉包を
保存するための幾何契約である．

% -------------------------------------------------------------------------------
\subsubsection{\texorpdfstring{D1 補正：$\sigma_\text{eff}(\bm{x})$ の空間場}{D1 補正：σ\_eff(x) の空間場}}
\label{sec:ridge_nu_sigma_field}
% -------------------------------------------------------------------------------

式~\eqref{eq:sigma_eff} の Gaussian 半幅
$\sigma_\text{eff}(\bm{x}) = \sigma_0 \cdot h(\bm{x})/h_\text{ref}$ は
格子幾何のみに依存するため，
\S\ref{sec:grid_gen} の格子点座標生成直後に
$\{\sigma_\text{eff}(\bm{x}_{i,j})\}_{i,j}$ として一度だけ定義する．
時間固定格子ではこの空間場は不変であり，追加の漸近計算量を生じない．

\noindent\textbf{幾何定義}：
\begin{itemize}
  \item 局所幾何平均 $h(\bm{x}) = \sqrt{h_x(i)\,h_y(j)}$ は
        本節ローカルな逆数メトリクス
        $\mathsf{X}_\xi = \mathrm{d}x/\mathrm{d}\xi = 1/J_x^{\mathrm{main}}$ から
        $h_x(i) = \mathsf{X}_\xi(\xi_i)\Delta\xi$ として得られる．
  \item 基準幅 $h_\text{ref} = (L_x L_y / (N_x N_y))^{1/2}$ は
        スカラ定数として格子生成時に決定．
  \item ALE（\S\ref{sec:grid_ale}）時間変化格子では
        $\sigma_\text{eff}$ を格子更新ごとに再計算する．
        壁面接触界面では式~\eqref{eq:ridge_wall_mirror} の
        鏡像 crossing も格子更新後に再生成する．
\end{itemize}

一様極限（$h(\bm{x}) \equiv h_\text{ref}$）で
$\sigma_\text{eff} \to \sigma_0$ となり一様格子版の既定と一致する．

% -------------------------------------------------------------------------------
\subsubsection{D2 補正：物理空間 Hessian は非一様 CCD で直接評価}
\label{sec:ridge_nu_hessian_field}
% -------------------------------------------------------------------------------

注記~\ref{rem:xi_hessian_forbidden}（$\xi$ 空間 Hessian の実用的困難性）により，
Ridge 検出 \eqref{eq:ridge_set_def} で必要な
Hessian $\mathbf{H}_{\xiridge}=\bnabla_x^2 \xiridge$ は
物理空間で直接評価しなければならない．
\S\ref{sec:ccd_metric} の非一様 CCD（座標変換法）は
ノード値から直接物理空間の第一・第二導関数を返す演算子
$\mathbf{D}_1^\text{CCD,nu}, \mathbf{D}_2^\text{CCD,nu}$ を提供するため，
Approach A（主経路）は以下のフル Hessian 行列で定義する：
\begin{equation}
  \mathbf{H}_{\xiridge,i,j}^{\text{nu}}
  =
  \begin{pmatrix}
    D_{xx}^{\text{CCD,nu}}\xiridge &
    D_{xy}^{\text{CCD,nu}}\xiridge \\
    D_{xy}^{\text{CCD,nu}}\xiridge &
    D_{yy}^{\text{CCD,nu}}\xiridge
  \end{pmatrix}_{i,j},
  \qquad
  D_{xy}^{\text{CCD,nu}}
  = D_y^{(1),\text{CCD,nu}}D_x^{(1),\text{CCD,nu}},
  \label{eq:ridge_nu_hessian}
\end{equation}
ここで軸ごとに \S\ref{sec:ccd_metric} 式~\eqref{eq:transform_2nd_correct} を
本節記号で書き直した
$\partial_x^2 = \mathsf{X}_\xi^{-2}\,\partial_\xi^2
  - (\mathsf{X}_{\xi\xi}/\mathsf{X}_\xi^3)\,\partial_\xi$
の修正済み行列を適用する
（$\mathsf{X}_{\xi\xi} = \mathrm{d}^2 x/\mathrm{d}\xi^2$；
主本文 $J_x^{\mathrm{main}} = 1/\mathsf{X}_\xi$ と $J_x^2 \partial_\xi^2 + J_x\partial_\xi J_x \partial_\xi$
の表現は数学的に等価）．
混合項 $D_{xy}^{\text{CCD,nu}}$ は §~\ref{sec:ccd_mixed} の逐次 1 次 CCD 適用で評価する．
Ridge 判定は §~\ref{sec:ridge_eikonal} の定義と同じく
$\lambda_{\min}(\mathbf{H}_{\xiridge}^{\text{nu}})<0$ を用い，
法線曲率形式で確認する場合も
$\hat{\bm{n}}^{\!\top}H\hat{\bm{n}}
= n_x^2H_{xx}+2n_xn_yH_{xy}+n_y^2H_{yy}$ として
混合項を保持する．

\noindent\textbf{Approach B（fallback）}：
物理空間二階 FD
\[
  [\partial_x^2\xiridge]_{i,j}
  = \frac{2}{h_x^+(i)+h_x^-(i)}
    \left[\frac{(\xiridge)_{i+1,j}-(\xiridge)_{i,j}}{h_x^+(i)}
          - \frac{(\xiridge)_{i,j}-(\xiridge)_{i-1,j}}{h_x^-(i)}\right]
\]
は 2 次精度に留まるが，Morse 非退化リッジ\textbf{かつ曲率半径 $R \gg h_\text{min}$}では
符号判定に影響しない（小曲率半径の薄いリッジでは $\Ord{h^2}$ 誤差が
$\hat{\bm{n}}^{\!\top}H\hat{\bm{n}}$ の符号を反転させうるため Approach A を推奨）．
混合成分 $H_{xy}$ は §~\ref{sec:ccd_mixed} と同じ 2 段適用で評価し，
対角成分だけに落とさない．
したがって二階 FD 経路は確認計算に限る．

% -------------------------------------------------------------------------------
\subsubsection{D3 補正：非一様 FMM の格子幅条件}
\label{sec:ridge_nu_fmm_geometry}
% -------------------------------------------------------------------------------

式~\eqref{eq:fmm_closed_form} の閉形式解
$d = (a_x/h_x^2 + a_y/h_y^2 + \sqrt{D})/(1/h_x^2 + 1/h_y^2)$ を
格子層で組み立てるには，
各ノード $(i,j)$ に対する隣接セル幅 $(h_x(i), h_y(j))$ を
FMM ヒープの priority 計算時に即座に参照する必要がある．
必要条件は以下：
\begin{itemize}
  \item \textbf{セル幅配列}：
        \S\ref{sec:grid_gen} の $\{x_i\}_i, \{y_j\}_j$ から
        $h_x[i] := x_i - x_{i-1}$，$h_y[j] := y_j - y_{j-1}$ を定義
        （\S\ref{sec:fccd_nu_geometry} の $H_i$ と同義；本節の $h_x[i] = H_i$ として参照）．
        FMM 内の各 tentative 更新は $O(1)$ テーブル参照のみ．
  \item \textbf{判別式負時の fallback}：
        $D<0$ のとき一次元単調更新
        $d = \min(a_x + h_x(i),\,a_y + h_y(j))$．
        コーナー近傍や急な密度勾配帯で発生し，
        粘性一意性は物理空間 Eikonal \eqref{eq:fmm_nonuniform} の
        弱解性質により保たれる．
  \item \textbf{サブセルシード}：
        実際の零集合 $S_h$ は一般にノード上に載らないため，
        符号反転セルの線形補間位置から最寄りノードへ
        物理空間距離でシード値を分配する．
        非一様格子では $(h_x(i),h_y(j))$ で正しく重み付ける必要があり，
        単位セル仮定では最大 $\alpha$ 倍の誤差を生む
        （$\alpha$ は格子密度比）．
        壁面接触界面では式~\eqref{eq:wall_seed_set} の
        $\mathcal{Z}_h(\phi_0;\partial\Omega_W)$ も同じ Dirichlet seed として扱い，
        exact-zero 壁ノードも seed に含める．
        一方，周期軸の端点は商空間同一性 $q_N=q_0$ で閉じる像であり，
        壁 seed や contact-line pinning の対象ではない．
\end{itemize}

\noindent この結線により FMM のワンパス計算量は
一様版と同じ $\mathcal{O}(N \log N)$ を維持し，
一軸あたり 1.0--1.2$\times$（$h_x[i]$ 参照の分）．

\noindent\textbf{並列化時の等価性要件．}
非一様 FMM は Accepted 集合から最小 tentative 値を順に確定する
因果的 Eikonal 解法であり~\cite{Sethian1996}，固定回数の sweeping や Jacobi 型緩和に置き換えると
式~\eqref{eq:fmm_nonuniform} の粘性解を同じ順序で近似したことにならない．
したがって並列化しても，距離場，Accepted/freezing 集合，
trial queue に相当する因果順序を保ち，同じ非一様 upwind 二次更新
\eqref{eq:fmm_closed_form} によって隣接点を緩和する必要がある．
これは高速化の細目ではなく，曲率・pressure-jump・BF 投影が共有する
signed-distance metric を定義する離散化契約である．

% -------------------------------------------------------------------------------
\subsubsection{\texorpdfstring{D4 補正：$\varepsilon_\text{local}(\bm{x})$ の空間場}{D4 補正：ε\_local(x) の空間場}}
\label{sec:ridge_nu_eps_field}
% -------------------------------------------------------------------------------

式~\eqref{eq:eps_local} の再構成幅
$\varepsilon_\text{local}(\bm{x}) = \varepsilon_\text{scale}\cdot\varepsilon_\xi\cdot h(\bm{x})$ を
D1 と同様に格子点上の空間場として定義する．
ここでの $\varepsilon_\text{local}$ は Ridge--Eikonal 再構成幅であり，
CSF 表面張力の $\delta_\varepsilon(\phi)$ に空間可変幅を使う許可ではない
（§~\ref{box:eps_xi_csf_warning}）．
$\xiridge$ から $\phi$ への再構成式
\begin{equation*}
  \phi(\bm{x}) = \operatorname{sgn}(\xiridge - \xi_\text{th})\cdot d_\mathcal{R}(\bm{x}),
  \qquad \xi_\text{th} = \xi_\text{ridge,min} - \varepsilon_\text{local}(\bm{x})
\end{equation*}
では閾値 $\xi_\text{th}$ がノードごとに変わるため，
ノードごとに $\varepsilon_\text{local}(\bm{x}_{i,j})$ を用いる．

\noindent\textbf{体積保存の監視}：
一様較正 $\varepsilon_\text{scale}=1.4$ を初期値として採用し，
$|\Delta V/V|>5\%$ を検知した時点で適応スケーリング
$\varepsilon_\text{scale}(\bm{x})=1.4\cdot h_\text{ref}/h_\text{min}$ に切替える
（閾値は \S\ref{sec:xi_sdf_reinit} の較正に基づく）．
界面適合格子族（\S\ref{sec:grid_density}）では
$h(\bm{x}) \approx h_\text{ref}$ が界面近傍で成立するため，
適応スケーリングは密度勾配帯のみで発動する．

% -------------------------------------------------------------------------------
\subsubsection{界面適合格子族との統合}
\label{sec:ridge_nu_interface_fitted}
% -------------------------------------------------------------------------------

\S\ref{sec:grid_density} のグリッド密度関数
$\omega_\text{grid}(\xi)$ を界面近傍で集中化すると，
D1--D4 の効果が次のように簡素化される：
\begin{itemize}
  \item \textbf{D1 $\sigma_\text{eff}$}：界面帯では $h(\bm{x}) \approx h_\text{min}$ 一定．
        Ridge 検出は等価密度帯内で実行されるため実効 $\sigma_\text{eff}$ は一様．
  \item \textbf{D2 Hessian}：界面法線 $\hat{\bm{n}}$ は密度勾配方向と
        ほぼ平行になる単純帯では対角寄与が支配的になりやすいが，
        離散化では $H_{xy}$ も保持して
        斜め界面・曲率帯での符号反転を避ける．
  \item \textbf{D3 FMM}：界面から距離 $\mathcal{O}(\varepsilon_\text{local})$ の
        narrow band のみで実行．$D<0$ fallback の発動率は 1\% 未満
        （本稿の実測値）．
  \item \textbf{D4 $\varepsilon_\text{local}$}：界面帯で一様値
        $\varepsilon_\text{local}\approx \varepsilon_\text{scale}\cdot\varepsilon_0$．
\end{itemize}
つまり界面適合格子では Ridge--Eikonal の非一様補正は
「界面帯での一様近似 + 密度勾配帯での正則化」として作用する．
ただし，これは D1--D4 の数式上の接続であり，
移動界面，分裂合体，分相 PPE，HFE，pressure-jump を同時に含む
長時間 NS 計算での標準閉包としての実証は別ゲートとして残る．
一方，壁面接触については式~\eqref{eq:wall_closed_interface}--\eqref{eq:contact_pinned_mass_correction}
により，幾何再構成が有限 gap を作らないことを離散契約として要求する．

% -------------------------------------------------------------------------------
\subsubsection{計算コスト・プレ計算・FCCD との接続}
\label{sec:ridge_nu_cost}
% -------------------------------------------------------------------------------

\noindent\textbf{プレ計算（1 回のみ）}：
$\sigma_\text{eff}[i,j]$，$h_x[i]$，$h_y[j]$，$\varepsilon_\text{local}[i,j]$ の
4 配列を格子構築時にキャッシュ（$\mathcal{O}(N_x N_y)$ メモリ）．
ALE モードでは格子更新ごとに再計算．

\noindent\textbf{ランタイムコスト}（再初期化 1 回）：
\begin{itemize}
  \item D2 非一様 CCD Hessian: $\mathcal{O}(N_x N_y)$（\S\ref{sec:ccd_metric}）
  \item D3 非一様 FMM: $\mathcal{O}(N_x N_y \log(N_x N_y))$
  \item D4 $\varepsilon_\text{local}$ テーブル参照: $\mathcal{O}(N_x N_y)$
\end{itemize}
合計は一様版と同じ漸近オーダー．
一軸あたり実行時間は一様版の 1.1--1.3$\times$．

\noindent\textbf{FCCD との接続}：
\S\ref{sec:ridge_fccd_handoff} で示した通り，
Ridge--Eikonal で再構成された $\phi$ は
\S\ref{sec:fccd_def}/\S\ref{sec:fccd_nonuniform} の FCCD 面中心追跡に
そのまま引き継がれる．
非一様 FCCD の係数 $(\mu_i, \lambda_i)$ と
本節の $\sigma_\text{eff}, \varepsilon_\text{local}$ は
\textbf{同じ格子幾何テーブル} $(h_x, h_y, \mathsf{X}_\xi, \mathsf{Y}_\eta)$ から派生するため，
格子層のプレ計算を一度行えば両アルゴリズムが同じキャッシュを共有できる
（主本文 $J_x^{\mathrm{main}}, J_y^{\mathrm{main}}$ は逆数 $1/\mathsf{X}_\xi, 1/\mathsf{Y}_\eta$ で得る）．

% ═══════════════════════════════════════════════════════════════════════════════
