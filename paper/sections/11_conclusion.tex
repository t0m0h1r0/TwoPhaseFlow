% paper/sections/11_conclusion.tex
\section{まとめと今後の課題}
\label{sec:conclusion}

\subsection{本稿の貢献のまとめ}

本稿では，気液二相流の高精度数値シミュレーションのための完全な定式化を示した．
採用した手法と，それが解決する具体的問題の対応を表\ref{tab:contributions}に整理する．

\begin{table}[ht]
\centering
\caption{各手法と解決する問題の対応（1章の失敗例と照合）}
\label{tab:contributions}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{42mm}p{38mm}l}
\toprule
解決する問題 & 採用手法 & 詳細 \\
\midrule
界面のにじみ・質量損失 & Conservative Level Set & 3章 \\
高精度微分（曲率計算） & CCD 法（$O(h^6)$） & 5章 \\
寄生流れの抑制 & CCD + CSF の整合的実装 & 2，5章 \\
チェッカーボード不安定 & Rhie--Chow 補正 & 6章 \\
変密度 PPE の安定求解 & 調和平均 + 仮想時間法 & 6，7章 \\
移流の数値振動 & WENO5 + TVD-RK3 & 8章 \\
\bottomrule
\end{tabular}
\end{table}

特に以下の3点が技術的に重要な貢献である：

\begin{enumerate}
  \item \textbf{質量保存性：}
        CLS 法の導入により，標準 Level Set 法の根本的弱点（非保存形移流による質量損失）を克服した．
        $\psi(1-\psi)$ 型の圧縮項と拡散項の競合が界面厚さを安定に維持し，
        長時間計算でも $\int\psi\,dV \approx \text{const}$ が保たれる（10章）．

  \item \textbf{高精度空間微分：}
        CCD 法の係数を，テイラー展開による厳密な代数的手続きで導出した（5章）．
        $O(h^6)$ 精度の曲率計算は，表面張力項の寄生流れを大幅に抑制する．

  \item \textbf{安定な圧力-速度連成：}
        Rhie--Chow 補正における調和平均密度の採用と，変密度 PPE の導出を整合的に示した（6，7章）．
        特に密度比 $\rho_l/\rho_g = O(1000)$ の場合，算術平均と調和平均の差が250倍に達することを
        定量的に示し，調和平均の必要性を明確にした（6章）．
\end{enumerate}

\subsection{本稿の位置付けと学習ロードマップ}

本稿は，気液二相流の CFD を初めて学ぶ読者のための橋渡し教材として設計されている．
以下の学習ロードマップを参考に，本稿の内容を段階的に習得することを推奨する．

\begin{tcolorbox}[defbox, title={学習ロードマップ}]
\textbf{ステップ1（前提知識）：}
単相 Navier--Stokes 方程式の基礎，Projection 法，有限差分法の基本

\textbf{ステップ2（本稿1--3章）：}
One-Fluid 定式化，CSF モデル，CLS 法による界面追跡の理解

\textbf{ステップ3（本稿5--7章）：}
CCD 法の理論と実装，Rhie--Chow 補正，変密度 PPE の導出

\textbf{ステップ4（本稿8--9章）：}
WENO5，時間積分，完全アルゴリズムの実装

\textbf{ステップ5（本稿10章）：}
4つのベンチマークによる段階的検証
\end{tcolorbox}

\subsection{今後の課題}

本稿で示した定式化は2次元の等温非圧縮流体を対象としている．
実問題への適用に向けた主要な拡張方向を以下に示す．

\begin{enumerate}
  \item \textbf{3次元実装と並列化：}
        3次元への拡張は概念的に直接的だが，CCD 法のブロック Thomas 法および
        Rhie--Chow 補正のフェイスフラックス計算を3方向に拡張する必要がある．
        大規模並列計算（MPI + OpenMP）での領域分割は，コンパクトスキームの
        非局所性（境界メッセージ通信）への対応が実装上の主要課題となる．

  \item \textbf{適応的格子細分化（AMR）との統合：}
        界面近傍のみを動的に高解像度化する AMR（Adaptive Mesh Refinement）と
        CLS 法を組み合わせることで，計算コストを大幅に削減できる．
        ただし，粗・密格子の境界での CCD スキームの整合性が技術的課題となる．

  \item \textbf{表面張力の陰的処理：}
        毛管波制約（$\Delta t_\sigma \propto \sqrt{\Delta x^3}$，8章）を除去するため，
        表面張力項を陰的に扱う手法（Implicit Surface Tension）の導入により，
        高表面張力問題での計算効率を向上させる．

  \item \textbf{相変化（蒸発・凝縮）の組み込み：}
        沸騰・蒸発問題では，界面でのエネルギー方程式と質量フラックスの結合が必要となる．
        CLS フレームワークは，界面での境界条件（Stefan 条件）の自然な実装を可能にする．

  \item \textbf{乱流モデルとの統合：}
        高 Reynolds 数流れでは，大渦シミュレーション（LES）または
        RANS モデルとの結合が必要である．
        変密度場での乱流モデリングは，特に気液界面近傍での渦粘性の扱いに課題がある．
\end{enumerate}

本稿の定式化が，気液二相流数値解析の研究・教育に貢献することを期待する．
