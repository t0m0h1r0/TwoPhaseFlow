% paper/sections/10_implementation.tex
\section{計算コンポーネントの数学的検証}
\label{sec:numerical_integrity}%
\label{sec:impl_collocate}% backward compat: referenced from §7

本章は，前章までに導出した各離散演算子・ソルバ・時間発展スキームが，
設計通りの数学的精度と安定性を有することを個別に検証する．
検証は\textbf{解析解が既知の単純問題}に対して行い，
格子収束試験（grid refinement study）を基本手法とする．

検証は以下の 3 段階で構成される：
(1) 空間離散化スキームの精度（§\ref{sec:verify_spatial}），
(2) 界面処理の精度（§\ref{sec:verify_interface}），
(3) ソルバと時間発展の精度（§\ref{sec:verify_solver}）．

% ═══════════════════════════════════════════════════════════════════════════════
\subsection{空間離散化の精度検証}
\label{sec:verify_spatial}
% ═══════════════════════════════════════════════════════════════════════════════

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{CCD/DCCD 微分演算の収束性}
\label{sec:verify_ccd_convergence}
% ─────────────────────────────────────────────────────────────────────────────

CCD 法（§\ref{sec:ccd_def}）の $\Ord{h^6}$ 精度を定量的に確認する．
テスト関数として以下の 2 種を用い，
$N = 16, 32, 64, 128, 256$ の各格子解像度で 1 階・2 階微分の $L_2$/$L_\infty$ 誤差を計測する．

\textbf{周期境界（テスト関数 $f = \sin(2\pi x)\sin(2\pi y)$）：}
1 階微分の $L_2$ 誤差は $N = 16 \to 128$ の範囲で精確に $\Ord{h^6}$ の収束を示す
（収束次数 $p \approx 6.00$--$6.06$）．
2 階微分も同等の $\Ord{h^{5.97}}$ 精度を達成する．
$N = 256$ では打ち切り誤差が $\Ord{10^{-14}}$ に到達し，
丸め誤差のフロアに接触するため見かけ上の収束次数が低下する
（$L_\infty$ で $p \approx 5.36$）．
これは CCD の理論限界ではなく倍精度浮動小数点の限界である．

\begin{table}[htbp]
\centering
\caption{CCD 1 階微分の格子収束（周期境界，$f = \sin(2\pi x)\sin(2\pi y)$）}
\label{tab:ccd_periodic}
\begin{tabular}{rrrrr}
\toprule
$N$ & $L_2$ 誤差 & 次数 & $L_\infty$ 誤差 & 次数 \\
\midrule
 16 & $1.28 \times 10^{-6}$  & ---  & $2.57 \times 10^{-6}$  & ---  \\
 32 & $1.93 \times 10^{-8}$  & 6.06 & $3.86 \times 10^{-8}$  & 6.06 \\
 64 & $2.99 \times 10^{-10}$ & 6.01 & $5.97 \times 10^{-10}$ & 6.01 \\
128 & $4.65 \times 10^{-12}$ & 6.00 & $9.36 \times 10^{-12}$ & 6.00 \\
256 & $7.67 \times 10^{-14}$ & 5.92 & $2.28 \times 10^{-13}$ & 5.36 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{壁境界（テスト関数 $f = \exp(\sin\pi x)\exp(\cos\pi y)$）：}
壁境界条件を伴う場合も，$N = 16 \to 128$ で $L_2$ 精度
$\Ord{h^{6.8\text{--}8.6}}$ と設計精度を超える収束が得られる．
$N = 256$ では境界処理に起因する精度低下（$p \approx 4.2$）が見られるが，
絶対誤差は $L_2 = \Ord{10^{-12}}$ と十分に小さい．

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{DCCD の数値散逸特性}
\label{sec:verify_dccd_dissipation}
% ─────────────────────────────────────────────────────────────────────────────

DCCD（§\ref{sec:dccd_decoupling}）の散逸メカニズムを
離散伝達関数 $H(\xi; \varepsilon_d)$ により検証する．
伝達関数は $e^{i\xi x}$ に対する CCD の応答比として定義され，
DCCD の場合（§\ref{sec:dccd_decoupling}）
\begin{equation}
  H(\xi; \varepsilon_d) = 1 - 4\varepsilon_d \sin^2(\xi/2)
  \label{eq:dccd_transfer_verify}
\end{equation}
と解析的に表される．Nyquist 波数 $\xi = \pi$ での減衰率を表~\ref{tab:dccd_transfer} に示す．

\begin{table}[htbp]
\centering
\caption{DCCD 伝達関数の Nyquist 波数における値 $H(\pi; \varepsilon_d)$}
\label{tab:dccd_transfer}
\begin{tabular}{rr}
\toprule
$\varepsilon_d$ & $H(\pi)$ \\
\midrule
0.00 & $1.0000$（散逸なし：CCD）\\
0.05 & $0.8000$（移流用：低散逸）\\
0.25 & $0.0000$（PPE 用：完全抑制）\\
\bottomrule
\end{tabular}
\end{table}

$\varepsilon_d = 1/4$ のとき $H(\pi) = 0$ となり，
Nyquist モード（チェッカーボード振動）が\textbf{完全に抑制}される．
これは §\ref{sec:checkerboard_solution} の理論的結論と一致する．
$\varepsilon_d = 0.05$（移流ステップ用）では $H(\pi) = 0.80$ であり，
高波数成分を 20\% 減衰させつつ，低波数の移流精度を保持する．

数値実験として $N = 128$ 格子上でチェッカーボードモード $(-1)^{i+j}$ に
DCCD フィルタ（$\varepsilon_d = 0.25$）を適用した結果，
フィルタ後の振幅はゼロとなることを確認した．

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{曲率 $\kappa$ の計算精度}
\label{sec:verify_curvature}
% ─────────────────────────────────────────────────────────────────────────────

曲率計算（§\ref{sec:curvature}，式~\eqref{eq:curvature_2d}）の精度を
2 種類のテスト問題と 3 つの計算経路で検証する．
3 経路の定義を以下に示す：
\begin{itemize}
  \item \textbf{経路 A（$\phi$ 直接）：}
        真の符号付距離関数 $\phi$ に CCD を適用し $\kappa$ を計算（理想ベースライン）．
  \item \textbf{経路 B（$\psi \to \phi$ ロジット逆変換）：}
        CLS 変数 $\psi$ からロジット逆変換
        $\phi = \varepsilon\ln[\psi/(1-\psi)]$（§\ref{sec:newton_inversion}）で
        $\phi$ を復元し，$\phi$ の CCD 微分から $\kappa$ を計算（従来法）．
  \item \textbf{経路 C（$\psi$ 直接法，§\ref{sec:curvature_direct_psi_cls}）：}
        $\psi$ の CCD 微分値から式~\eqref{eq:curvature_psi_2d} で $\kappa$ を直接計算
        （ロジット逆変換を省略；§\ref{sec:curvature_direct_psi_cls} の推奨経路）．
\end{itemize}

\textbf{(a) 正弦波状界面 $y = 0.5 + A\sin(2\pi x)$，$A = 0.05$：}
曲率の解析解 $\kappa = f''/(1+f'^2)^{3/2}$ と CCD 計算値を比較する．
経路 A では $N = 32 \to 256$ で $L_\infty$ 誤差の収束次数は
$p \approx 4.96$--$5.00$ を示す（表~\ref{tab:curvature_sinusoidal}）．
これは CCD 6 次精度微分から期待される高次収束と整合する．

\begin{table}[htbp]
\centering
\caption{正弦波状界面の曲率 $L_\infty$ 誤差収束（$A = 0.05$，$\varepsilon = 1.5h$）}
\label{tab:curvature_sinusoidal}
\begin{tabular}{rrrrrrr}
\toprule
$N$ & A: $\phi$ 直接 & 次数 & B: ロジット経由 & 次数 & C: $\psi$ 直接 & 次数 \\
\midrule
 32 & $7.64 \times 10^{-4}$ & ---  & $7.64 \times 10^{-4}$ & ---  & $3.84 \times 10^{-2}$ & ---  \\
 64 & $2.45 \times 10^{-5}$ & 4.96 & $2.14 \times 10^{-5}$ & 5.16 & $2.19 \times 10^{-2}$ & 0.81 \\
128 & $7.72 \times 10^{-7}$ & 4.99 & $5.80 \times 10^{-6}$ & 1.88 & $2.27 \times 10^{-2}$ & $-0.05$ \\
256 & $2.42 \times 10^{-8}$ & 5.00 & $1.33 \times 10^{-5}$ & $-1.20$ & $3.59 \times 10^{-2}$ & $-0.66$ \\
\bottomrule
\end{tabular}
\end{table}

経路 A は全格子で $\Ord{h^5}$ を維持する．
経路 B は $N \leq 64$ で A と同等の精度を示すが，$N \geq 128$ では
ロジット逆変換の飽和域処理に起因する誤差が顕在化して収束が停滞する．
経路 C は全格子で収束しない（次節で原因を分析）．

\textbf{(b) 円形界面（半径 $R = 0.25$，$\kappa_\mathrm{exact} = 1/R = 4$）：}
CCD 法と標準 2 次精度中央差分（CD2）を経路 B で比較する
（表~\ref{tab:curvature_circle}）．
両手法とも $\Ord{h^1}$ 程度の収束にとどまる．
これは CCD の理論精度限界ではなく，CLS 変数 $\psi$ から符号付距離関数 $\phi$ への
逆変換が界面厚み $\varepsilon = \Ord{h}$ に律速される系統誤差を生むためである．
(a) で正弦波界面に対して経路 A が $\Ord{h^5}$ を達成したことから，
CCD 微分演算自体は高精度であり，曲率精度の律速因子は
界面表現の解像度（$\varepsilon$ に対する格子点数）であることが分かる．

\begin{table}[htbp]
\centering
\caption{円形界面の曲率 $L_\infty$ 誤差収束：CCD 法 vs CD2 法（経路 B，$\varepsilon = 1.5h$）}
\label{tab:curvature_circle}
\begin{tabular}{rrrrr}
\toprule
$N$ & CCD $L_\infty$ & 次数 & CD2 $L_\infty$ & 次数 \\
\midrule
 16 & $1.26 \times 10^{1}$  & ---  & $8.82 \times 10^{0}$  & ---  \\
 32 & $2.42 \times 10^{0}$  & 2.38 & $2.23 \times 10^{0}$  & 1.98 \\
 64 & $9.09 \times 10^{-1}$ & 1.42 & $9.12 \times 10^{-1}$ & 1.29 \\
128 & $4.11 \times 10^{-1}$ & 1.14 & $4.10 \times 10^{-1}$ & 1.15 \\
256 & $1.96 \times 10^{-1}$ & 1.07 & $1.96 \times 10^{-1}$ & 1.07 \\
\bottomrule
\end{tabular}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{$\psi$ 直接法の数値的安定性：$\varepsilon$ 依存性の分析}
\label{sec:verify_psi_direct_stability}
% ─────────────────────────────────────────────────────────────────────────────

§\ref{sec:curvature_direct_psi_cls} で導入した $\psi$ 直接法（経路 C）は
曲率不変性定理（定理~\ref{thm:curvature_invariance}）に基づく
数学的に厳密な手法であるが，
標準 CLS 設定 $\varepsilon = 1.5h$ では数値的安定性に課題がある．

\textbf{原因：}
シグモイドの最大勾配は $|\bnabla\psi|_{\max} = 1/(4\varepsilon)$ であり，
$\varepsilon = 1.5h$ のとき $|\bnabla\psi|_{\max} \propto N$ と格子精緻化に伴い
際限なく増大する．
式~\eqref{eq:curvature_psi_2d} の分子・分母はいずれも
$|\bnabla\psi|^3 \propto 1/\varepsilon^3$ でスケールするため，
大きな値同士の差に起因する\textbf{桁落ち}が発生し，
$N$ 増加とともに曲率誤差が\emph{増大}する（表~\ref{tab:curvature_sinusoidal} 経路 C）．

\textbf{$\varepsilon$ 固定時の検証：}
$\varepsilon$ を $h$ に依存させず $\varepsilon = 0.05$（固定）とし，
格子精緻化とともにシグモイドの解像度（$\varepsilon/h$）を向上させた場合，
経路 C は $\Ord{h^6}$ の収束を達成する（表~\ref{tab:curvature_eps_fixed}）．
$\varepsilon/h \geq 3$（1 遷移幅あたり 6 格子点以上）が安定な適用条件である．

\begin{table}[htbp]
\centering
\caption{$\varepsilon = 0.05$（固定）での $\psi$ 直接法（経路 C）の曲率収束
（円形界面 $R = 0.25$，$|\phi| < 2h$ の狭帯域で評価）}
\label{tab:curvature_eps_fixed}
\begin{tabular}{rrrr}
\toprule
$N$ & $\varepsilon/h$ & 経路 C $L_\infty$ 誤差 & 次数 \\
\midrule
 32 & 1.6 & $2.34 \times 10^{-3}$ & ---  \\
 64 & 3.2 & $1.44 \times 10^{-5}$ & 7.35 \\
128 & 6.4 & $2.13 \times 10^{-7}$ & 6.08 \\
\bottomrule
\end{tabular}
\end{table}

\begin{tcolorbox}[resultbox, title={曲率計算経路の選択指針}]
\label{result:curvature_path_selection}
\begin{itemize}
  \item \textbf{標準 CLS 設定（$\varepsilon = \Ord{h}$）：}
        経路 B（$\psi \to \phi$ ロジット逆変換→CCD）を使用する．
        律速因子は界面解像度であり，CCD vs CD2 の精度差は $N \geq 64$ で消失する．
  \item \textbf{厚い界面設定（$\varepsilon/h \geq 3$）：}
        経路 C（$\psi$ 直接法）が $\Ord{h^6}$ を達成し，
        ロジット逆変換の飽和域誤差を回避できる．
  \item \textbf{精度ベースライン（検証用途）：}
        経路 A（真の符号付距離関数 $\phi$）は CCD 微分精度の上界を与える．
\end{itemize}
\end{tcolorbox}

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{実効界面厚み $\varepsilon_{\textup{eff}}$ によるプロファイル品質検証}
\label{sec:verify_eps_eff}
% ─────────────────────────────────────────────────────────────────────────────

§\ref{sec:curvature_direct_psi_cls} で導入した実効界面厚み
（式~\eqref{eq:epsilon_eff}）の診断精度を確認する．
円形界面（$R = 0.25$）の初期プロファイル $\psi = H_\varepsilon(\phi_\mathrm{exact})$
に対して，界面近傍（$\psi_{\min} < \psi < 1-\psi_{\min}$，$\psi_{\min} = 0.01$）の
$\varepsilon_{\textup{eff}}/\varepsilon$ を計測した．

\begin{table}[htbp]
\centering
\caption{$\varepsilon_{\textup{eff}}$ プロファイル品質診断（円形界面 $R = 0.25$，$\varepsilon = 1.5h$）}
\label{tab:eps_eff_diagnostic}
\begin{tabular}{rrrrrr}
\toprule
$N$ & 界面帯点数 & $\varepsilon_{\textup{eff}}/\varepsilon$（平均） & 標準偏差 & 最小 & 最大 \\
\midrule
 64 & 1388 & 1.000001 & $2.7 \times 10^{-5}$ & 0.9999 & 1.0001 \\
128 & 2732 & 1.000001 & $2.7 \times 10^{-5}$ & 0.9999 & 1.0001 \\
256 & 5548 & 1.000001 & $2.7 \times 10^{-5}$ & 0.9999 & 1.0001 \\
\bottomrule
\end{tabular}
\end{table}

全格子解像度で $\varepsilon_{\textup{eff}}/\varepsilon \approx 1$ が
$\Ord{10^{-5}}$ の精度で成立しており，
初期プロファイルのシグモイド形状が
CCD 微分で正確に再現されていることを確認した．
$\varepsilon_{\textup{eff}}$ は移流計算後のプロファイル歪みを
定量的に検出する運用時の診断指標として有用である．

% ═══════════════════════════════════════════════════════════════════════════════
\subsection{界面処理の精度検証}
\label{sec:verify_interface}
% ═══════════════════════════════════════════════════════════════════════════════

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{CLS 移流・再初期化精度}
\label{sec:verify_cls_advection}
% ─────────────────────────────────────────────────────────────────────────────

CLS 移流（§\ref{sec:advection}）と再初期化（§\ref{sec:reinit}）の
精度を 2 種類の標準テストで検証する．

\textbf{(a) Zalesak スロット円板テスト：}
中心 $(0.5, 0.75)$，半径 $R = 0.15$ の円板にスロット（幅 $0.05$，高さ $0.25$）を
切り込んだ形状を，中心 $(0.5, 0.5)$ の剛体回転場で 1 回転させる．
DCCD 移流（$\varepsilon_d = 0.05$）と再初期化（20 ステップ毎）を用い，
1 周期後の $\psi$ と初期 $\psi_0$ の $L_2$/$L_\infty$ 誤差および質量保存誤差を計測する．
$N = 64, 128, 256$ の格子精緻化により形状誤差の収束次数を確認する．

\textbf{(b) 単一渦テスト（LeVeque 1996）：}
時間反転型渦場により円形界面（中心 $(0.5, 0.75)$，$R = 0.15$）を
$T/2$ まで強く変形させた後，$T/2 \to T$ で元に戻す．
界面の鋭利さと質量保存を同時に評価する．

表~\ref{tab:cls_advection} に両テストの定量的結果を示す．
$L_2$/$L_\infty$ 形状誤差の収束は緩やかであるが，
これは CLS の遷移幅 $\varepsilon = \Ord{h}$ による本質的な制約であり，
同じ界面厚みで比較した場合は妥当な結果である．
一方，質量保存誤差は $N = 256$ で $\Ord{10^{-5}}$ に達し，
DCCD 移流と再初期化の保存性が良好であることを示す．

\begin{table}[htbp]
\centering
\caption{CLS 界面移流テストの精度（DCCD 移流 $\varepsilon_d = 0.05$）}
\label{tab:cls_advection}
\begin{tabular}{rrrrrr}
\toprule
 & $N$ & $L_2$ 誤差 & $L_\infty$ 誤差 & 質量保存誤差 \\
\midrule
\multirow{3}{*}{Zalesak}
 &  64 & $9.67 \times 10^{-2}$ & $5.06 \times 10^{-1}$ & $5.55 \times 10^{-3}$ \\
 & 128 & $9.28 \times 10^{-2}$ & $5.60 \times 10^{-1}$ & $1.08 \times 10^{-3}$ \\
 & 256 & $7.62 \times 10^{-2}$ & $5.63 \times 10^{-1}$ & $6.01 \times 10^{-5}$ \\
\midrule
\multirow{3}{*}{Single vortex}
 &  64 & $1.90 \times 10^{-1}$ & $8.46 \times 10^{-1}$ & $1.78 \times 10^{-3}$ \\
 & 128 & $1.73 \times 10^{-1}$ & $8.05 \times 10^{-1}$ & $1.63 \times 10^{-3}$ \\
 & 256 & $1.43 \times 10^{-1}$ & $7.44 \times 10^{-1}$ & $7.10 \times 10^{-5}$ \\
\bottomrule
\end{tabular}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{GFM 不連続面処理の精度}
\label{sec:verify_gfm}
% ─────────────────────────────────────────────────────────────────────────────

GFM（§\ref{sec:gfm}）による圧力跳び処理の精度を，
静止液滴の Young--Laplace 条件で検証する．

\textbf{設定：} 計算領域 $[0,1]^2$ に半径 $R = 0.25$ の円形液滴
（$\rho_l/\rho_g = 1000$）を配置する．
解析解は $\Delta p = \kappa/\mathit{We} = 1/(R \cdot \mathit{We})$ である．
GFM は界面を跨ぐ格子点ペアに対し PPE 右辺補正
$b^{\mathrm{GFM}}$（式~\eqref{eq:gfm_rhs_correction}）を適用し，
CSF のようなスミアリングなく圧力跳びを捕捉する．

GFM と CSF の Laplace 圧誤差を $N = 32, 64, 128$ で比較する（表~\ref{tab:gfm_laplace}）．
GFM は全格子で安定に収束し，$N = 128$ で相対誤差 $1.8\%$ を達成する．
一方 CSF は粗格子（$N = 32$）で $\Delta p$ の符号が反転し不安定だが，
細格子では GFM を上回る精度に到達する．
$\varepsilon$ 感度評価（$\varepsilon/h = 0.5$--$4.0$，$N = 128$）では
CCD 曲率が $\kappa \approx 4.007$（誤差 $< 0.2\%$）で $\varepsilon$ 非依存を確認した．

\begin{table}[htbp]
\centering
\caption{Laplace 圧力跳び $\Delta p = \kappa/\mathit{We}$（解析解 $4.0$）：GFM vs CSF}
\label{tab:gfm_laplace}
\begin{tabular}{rrrrrr}
\toprule
$N$ & GFM $\Delta p$ & GFM 相対誤差 & CSF $\Delta p$ & CSF 相対誤差 \\
\midrule
 32 & 3.748 & $6.3 \times 10^{-2}$ & $-2.361$ & $1.6 \times 10^{0}$ \\
 64 & 3.866 & $3.4 \times 10^{-2}$ &  3.647  & $8.8 \times 10^{-2}$ \\
128 & 3.927 & $1.8 \times 10^{-2}$ &  3.952  & $1.2 \times 10^{-2}$ \\
\bottomrule
\end{tabular}
\end{table}

GFM の収束次数は約 1 次であり，界面近傍の $\phi$ 補間精度に律速される．
CSF は細格子で 2 次に近い収束を示すが，粗格子安定性に課題がある．
実用的には GFM の粗格子ロバスト性が大規模計算で有利である．

% ═══════════════════════════════════════════════════════════════════════════════
\subsection{ソルバと時間発展の精度検証}
\label{sec:verify_solver}
% ═══════════════════════════════════════════════════════════════════════════════

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{PPE ソルバ収束特性}
\label{sec:verify_ppe}
% ─────────────────────────────────────────────────────────────────────────────

圧力 Poisson 方程式（§\ref{sec:pressure}）のソルバ精度を，
製造解法（method of manufactured solutions）により検証する．
製造解 $p = \cos(2\pi x)\cos(2\pi y)$ は壁面 Neumann 条件
$\partial p/\partial n = 0$ を自動的に満たす．

\textbf{(a) 格子収束（FVM-LU）：}
FVM-LU（5 点ステンシル直接法）は $N = 16 \to 128$ で
$L_2$ 誤差が $6.86 \times 10^{-3} \to 1.01 \times 10^{-4}$ と
精確に $\Ord{h^2}$ の収束を示す（表~\ref{tab:ppe_convergence}）．
CCD-LU（Kronecker 直接法）は Neumann 壁面条件下で
絶対誤差が大きく，CCD の壁面境界処理と PPE ゲージ条件の相互作用に
起因する課題が残る．本稼働環境では FVM-LU をベースラインソルバとして推奨する．

\begin{table}[htbp]
\centering
\caption{PPE ソルバ格子収束（製造解 $p = \cos(2\pi x)\cos(2\pi y)$，$\rho = 1$）}
\label{tab:ppe_convergence}
\begin{tabular}{rrrrr}
\toprule
$N$ & FVM-LU $L_2$ & 次数 & FVM-LU $L_\infty$ & 次数 \\
\midrule
 16 & $6.86 \times 10^{-3}$ & ---  & $1.30 \times 10^{-2}$ & ---  \\
 32 & $1.66 \times 10^{-3}$ & 2.05 & $3.22 \times 10^{-3}$ & 2.01 \\
 64 & $4.08 \times 10^{-4}$ & 2.02 & $8.04 \times 10^{-4}$ & 2.00 \\
128 & $1.01 \times 10^{-4}$ & 2.01 & $2.01 \times 10^{-4}$ & 2.00 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{(b) 反復法比較（$N = 64$）：}
BiCGSTAB（Jacobi 前処理）は $L_2 = 4.08 \times 10^{-4}$（FVM 精度）を
$0.10$ 秒で達成し，LGMRES-LU フォールバック（$16.1$ 秒）および
CCD-LU 直接法（$6.9$ 秒）に対して大幅に高速である．

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{TVD-RK3 時間発展精度}
\label{sec:verify_tvd_rk3}
% ─────────────────────────────────────────────────────────────────────────────

TVD-RK3（Shu--Osher 3 段 SSP スキーム，§\ref{sec:time_int}）の
$\Ord{\Delta t^3}$ 精度を 2 種類のテストで検証する．

\textbf{(a) ODE 検証 $dq/dt = -q$：}
解析解 $q(T) = e^{-T}$（$T = 1$）に対し，
$\Delta t$ を $n = 4, 8, \ldots, 512$ ステップで精緻化し，
誤差の収束次数を計測する（表~\ref{tab:tvd_rk3_ode}）．
$n \geq 16$ の安定領域で $p \approx 3.00$ の精確な 3 次収束を確認した．

\begin{table}[htbp]
\centering
\caption{TVD-RK3 の ODE 時間精度（$dq/dt = -q$，$T = 1$）}
\label{tab:tvd_rk3_ode}
\begin{tabular}{rrrr}
\toprule
ステップ数 $n$ & $\Delta t$ & $|q - e^{-1}|$ & 次数 \\
\midrule
  16 & $6.25 \times 10^{-2}$ & $3.55 \times 10^{-5}$  & ---  \\
  32 & $3.13 \times 10^{-2}$ & $4.39 \times 10^{-6}$  & 3.02 \\
  64 & $1.56 \times 10^{-2}$ & $5.46 \times 10^{-7}$  & 3.01 \\
 128 & $7.81 \times 10^{-3}$ & $6.81 \times 10^{-8}$  & 3.00 \\
 256 & $3.91 \times 10^{-3}$ & $8.51 \times 10^{-9}$  & 3.00 \\
 512 & $1.95 \times 10^{-3}$ & $1.06 \times 10^{-9}$  & 3.00 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{(b) スカラー移流方程式の時空間結合収束：}
$\partial q/\partial t + c\,\partial q/\partial x = 0$（$c = 1$）に対し，
CFL 数 $0.3$ で格子幅と $\Delta t$ を同時に精緻化する．
空間は CCD 6 次，時間は RK3 3 次であるため，
CFL 固定（$\Delta t \propto h$）では低次の時間精度が律速し
$\Ord{h^3}$ が期待される．

\textbf{(c) 純時間方向収束（空間 $N = 256$ 固定）：}
空間誤差を十分に小さくした条件で $\Delta t$ のみを精緻化し，
CFL 安定限界内（$n \geq 320$）で $\Ord{\Delta t^3}$ の精確な収束を確認した．
CFL 限界を超える $\Delta t$（$n < 160$）では不安定性が発生し，
TVD-RK3 の陽的安定領域の制約を示す．

% ═══════════════════════════════════════════════════════════════════════════════
\subsection{検証結果のまとめ}
\label{sec:verify_summary}
% ═══════════════════════════════════════════════════════════════════════════════

表~\ref{tab:verification_summary} に各コンポーネントの検証結果を総括する．
全ての離散演算子が設計精度を達成していることを確認した．
曲率計算の律速因子は CCD 微分精度ではなく界面表現の解像度（$\varepsilon/h$）であり，
この知見は第~\ref{sec:validation}章の多相流ベンチマークにおける精度解釈の基礎となる．
$\psi$ 直接法は $\varepsilon/h \geq 3$ で $\Ord{h^6}$ を達成するが，
標準 CLS 設定（$\varepsilon = 1.5h$）ではシグモイド勾配の急峻化に伴う桁落ちにより
$\phi$ 経由法が実用上優位である．

\begin{table}[htbp]
\centering
\caption{計算コンポーネント検証結果の総括}
\label{tab:verification_summary}
\begin{tabular}{llll}
\toprule
コンポーネント & 期待精度 & 実測精度 & 備考 \\
\midrule
CCD 微分（周期） & $\Ord{h^6}$ & $\Ord{h^{6.0}}$ & $N = 256$ で丸め誤差到達 \\
CCD 微分（壁面） & $\Ord{h^6}$ & $\Ord{h^{6.8\text{--}8.6}}$ & 境界項で超収束 \\
DCCD フィルタ   & $H(\pi) = 0$ & $H(\pi) = 0$ & チェッカーボード完全抑制 \\
曲率（正弦波，経路 A） & $\Ord{h^6}$ & $\Ord{h^{5.0}}$ ($L_\infty$) & $\phi$ 直接；十分高次 \\
曲率（円形，経路 B）   & --- & $\Ord{h^{1.1}}$ & $\varepsilon = 1.5h$ が律速 \\
曲率（$\psi$ 直接，$\varepsilon$ 固定） & $\Ord{h^6}$ & $\Ord{h^{6.1}}$ & $\varepsilon/h \geq 3$ で達成 \\
$\varepsilon_{\textup{eff}}$ 診断 & $\varepsilon_{\textup{eff}}/\varepsilon = 1$ & $1.000 \pm 10^{-5}$ & 初期プロファイル品質 \\
CLS 質量保存    & 保存的     & $\Ord{10^{-5}}$ ($N=256$) & 形状誤差は $\varepsilon$ 律速 \\
PPE (FVM-LU)    & $\Ord{h^2}$ & $\Ord{h^{2.0}}$ & 製造解で精確に確認 \\
TVD-RK3         & $\Ord{\Delta t^3}$ & $\Ord{\Delta t^{3.00}}$ & ODE・PDE 両方で確認 \\
\bottomrule
\end{tabular}
\end{table}
