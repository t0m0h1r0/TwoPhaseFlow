% paper/sections/07_advection.tex
%
% §6  CLS 移流の離散化：Dissipative CCD スキームの設計根拠
%     WENO5 は参照スキームとして付録~\ref{app:weno5} に収録する．
%
\section{CLS 界面追跡の空間離散化：移流・再初期化への CCD 適用}
\label{sec:discretization_modules}
\label{sec:advection}% backward-compat: 既存 \ref{sec:advection} を保持

本章では，§~\ref{sec:strategy} で設計した CCD エンジンを各物理項に適用し，
それぞれどのような恩恵を得るかを詳述する．
CLS 移流方程式（§~\ref{sec:levelset}，式~\eqref{eq:cls_advection}の再掲）は
\begin{equation}
  \frac{\partial\psi}{\partial t} + \bnabla\cdot(\psi\bm{u}) = 0
  \label{eq:cls_advection_ref}
\end{equation}
である．

\noindent\textbf{本章の対象範囲：}
本章では CLS 移流・再初期化と CLS 側安定性制約を詳述する．
FCCD による BF 整合移流 Option B/C は §~\ref{sec:fccd_advection}（§6.3）で展開し，
界面張力項（CSF モデルへの FCCD 適用）は §~\ref{sec:fccd_bf_sub_system}（§7.4）で，
粘性項（変密度 3 層 Hermite 評価）は §~\ref{sec:viscous_3layer}（§6.6）で詳述する．

\begin{enumerate}
  \item \textbf{§~\ref{sec:cls_on_nonuniform}（非一様格子上の CLS 演算）：}
        DCCD フィルタの $\xi$ 空間適用と $\psi$ 補間，空間可変 Heaviside 幅を論じる．
  \item \textbf{§~\ref{sec:advection_motivation}（移流項：CLS $\psi$ 用 DCCD；運動量用 UCCD6；比較対象 WENO5 は付録~\ref{app:weno5}）：}
        CLS $\psi$ 移流は高周波ノイズを狙い撃ちする「スペクトルフィルタ」として DCCD を用い，
        運動量 $(u,v)$ 移流は演算子内部散逸の UCCD6（§~\ref{sec:uccd6_def}，L1/L2/L3 既定）が担う．
  \item \textbf{§~\ref{sec:cls_compression}（CLS 再初期化）：}
        再初期化方程式の圧縮項を Dissipative CCD と Crank--Nicolson 法で離散化する．
  \item \textbf{§~\ref{sec:stability}（安定性制約）：}
        時間刻み $\Delta t$ の CFL・拡散安定条件と推奨値をまとめる．
\end{enumerate}

% ─────────────────────────────────────────────────────────────────────────────
\subsection{非一様格子上の CLS 演算}
\label{sec:cls_on_nonuniform}
% ─────────────────────────────────────────────────────────────────────────────

DCCD フィルタ（§~\ref{sec:dissipative_ccd}）を非一様格子上で適用する際には，
\textbf{計算空間 $\xi$ 上でフィルタを実行し，事後にメトリクス $J_x = \partial\xi/\partial x$ を適用}する．
物理空間 $x$ 上での直接フィルタリングは不均等な格子幅により
高周波減衰の一様性が崩れるため，$\xi$ 空間一様格子上での操作が必須である．
CCD ソルバーは計算空間で求解後に \texttt{\_apply\_metric} で物理空間微分を返すため，
フィルタは計算空間微分 $(f'_\xi, f''_\xi)$ の段階で適用し，
メトリクス変換はフィルタ適用後に行う（§~\ref{sec:algo_flow}, Step 1--4）．

格子再構築時の CLS 場 $\psi$ の転写には，
$\psi$ の\textbf{直接線形補間}が $\phi$ 経由の高次補間より優位である．
$\phi$ 3次補間ではロジット空間でのオーバーシュートと飽和域クリップにより
質量誤差が $\Ord{10^{-2}}$（3\%）に達するのに対し，
$\psi$ 線形補間はモノトン性（$\psi \in [0,1]$ 保持）から $\Ord{10^{-5}}$（0.005\%）に抑制される
（§~\ref{sec:cls_conservation} で定量検証）．

\paragraph{\texorpdfstring{計算空間 $\xi$ ベースの Heaviside 幅}{計算空間 xi ベースの Heaviside 幅}}

非一様格子では一様な $\varepsilon = C_\varepsilon h$ の代わりに，
局所格子幅に比例する空間可変場
\begin{equation}
  \varepsilon(\bx) = N_{\varepsilon,\text{cells}} \cdot h_{\mathrm{local}}(\bx),
  \quad h_{\mathrm{local}}(i,j) = \max(h_x(i),\, h_y(j))
  \label{eq:eps_xi_cells}
\end{equation}
を用いることができる（$N_{\varepsilon,\text{cells}}$ は $\xi$ 空間でのセル数）．
界面近傍（細かいセル）では $\varepsilon$ が小さく，遠方（粗いセル）では大きくなるが，
$\xi$ 空間では常に $N_{\varepsilon,\text{cells}}$ セル幅で Heaviside 遷移が解像される．
一様格子では $h_{\mathrm{local}} = h$ となり従来のスカラー $\varepsilon$ と一致する．

\begin{tcolorbox}[warnbox, title={空間可変 $\varepsilon(\bx)$ と CSF 表面張力の非互換性}]
\label{box:eps_xi_csf_warning}
式~\eqref{eq:eps_xi_cells} の空間可変 $\varepsilon$ は，
CSF 表面張力モデル（デルタ関数型体積力）との組み合わせで
寄生流れを著しく増幅させる（§~\ref{sec:local_eps_validation} で定量検証）．
原因は，$\varepsilon$ の空間変動に伴うデルタ関数 $\delta_\varepsilon(\phi)$
の振幅不整合であり，細い格子と粗い格子の境界で非物理的な表面力が発生する．

$N_{\varepsilon,\text{cells}}$ が有効なのは，\textbf{表面張力を GFM（Ghost Fluid Method）で
計算する場合に限られる}：デルタ関数を介さず圧力ジャンプを直接課すため，
振幅不整合の影響を受けない（§~\ref{sec:future_work} 参照）．
加えて，$N_{\varepsilon,\text{cells}} \ge 1.5$ が必要である：
それ未満では界面近傍の細かいセルで Heaviside 遷移が 1 セル幅未満となり
解像不足になる．
\end{tcolorbox}

\noindent\textbf{設計上の注意：}
§~\ref{sec:dissipative_ccd} で導入した DCCD フィルタは本章でも再利用するが，
動作モードが異なる．
速度・圧力場では界面適応スイッチ $S(\psi) = (2\psi-1)^2$ により
界面上でフィルタを OFF にしたが（§~\ref{sec:dccd_decoupling}），CLS 移流では $\psi \approx 0.5$（界面）付近こそ
安定化が最も必要な領域であるため，\textbf{一様強度}
$\varepsilon_d^{\mathrm{adv}} = 0.05$ を全格子点に適用する
（詳細は §~\ref{sec:advection_dccd_design}）．

% ─────────────────────────────────────────────────────────────────────────────
\subsection{移流項：CLS $\psi$ 用 Dissipative CCD と運動量用 UCCD6}
\label{sec:advection_motivation}
% ─────────────────────────────────────────────────────────────────────────────

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{移流スキーム選択の設計根拠：WENO5 単独の不適合と CLS に DCCD を採る理由}
\label{sec:advection_weno5_critique}
% ─────────────────────────────────────────────────────────────────────────────

WENO5~\cite{JiangShu1996} は圧縮性流体力学の衝撃波捕捉向けに設計されたスキームであり，
CLS 界面移流には以下の 3 点で非適合である
（詳細な定式化と性質は付録~\ref{app:weno5} を参照）：

\begin{enumerate}
  \item \textbf{過剰散逸：}
        CLS の $\psi = H_\varepsilon(\phi)$ は滑らかな関数であり真の不連続を含まないが，
        WENO5 の滑らかさ指標はこの勾配を「不連続の兆候」と誤検知し，
        不必要な非線形散逸を常時付加する．
        結果として界面の微細構造（液膜・せん断巻き込み）が非物理的に平滑化される．

  \item \textbf{Balanced-Force 精度の侵食：}
        WENO5 の非線形重みが $\psi$ プロファイルに振動・ぼやけを残すことで，
        Balanced-Force 条件（§~\ref{sec:balanced_force}）が確保する
        寄生流れ $\Ord{h^6}$ の高精度を実用上無効化する危険がある．

  \item \textbf{ステンシル非整合：}
        本フレームワークは全空間離散化に CCD の 3 点コンパクトステンシルを用いるが，
        WENO5 の 5 点ステンシルを混在させると
        打ち切り誤差の非整合（$\Ord{h^5}$ が $\Ord{h^6}$ を律速）が生じ，
        境界条件処理の一元化も失われる．
\end{enumerate}

\noindent
\textbf{結論}：純 WENO5 単独は CLS 界面移流に不適合であるため，
本稿は CLS 移流に \textbf{Dissipative CCD（DCCD）}を採用する．
すなわち CCD で計算した発散形フラックス $f' = \partial(\psi\bu)/\partial x$ に
スペクトル散逸 $\varepsilon_d h^2 D^2$（$f'$ への作用：$f'_{i+1}-2f'_i+f'_{i-1}$）
を後段で課す（§\ref{sec:cls_stages} Stage A）．
CCD は線形・コンパクト・低散逸スキームであり，
CLS の滑らかな $\psi$ プロファイルに対して界面構造を追加散逸なく長時間保持でき，
支配方程式全体の空間離散化を単一構造で統一する．
WENO5 は \emph{比較参照スキーム}として付録~\ref{app:weno5} に収録するに留め，
本稿の主実装には用いない（§\ref{sec:verify_weno5_vs_dccd} で DCCD と WENO5 を定量比較）．

% ─────────────────────────────────────────────────────────────────────────────
\subsubsection{気液界面移流の困難と純 CCD の限界}
\label{sec:advection_ccd_limit}

$\psi$ は界面付近で $0$ から $1$ へと急激に変化する（界面幅 $\sim 2\varepsilon$）．
この急峻なプロファイルを移流方程式~\eqref{eq:cls_advection_ref}で輸送するとき，
\textbf{純中心差分を前進 Euler 型の時間積分と組み合わせると線形安定性を失う}．
虚軸安定域を持つ Runge--Kutta 法では許容 CFL が存在しうるため，
以下ではまず最も単純な前進 Euler との組合せで純 CCD の限界を確認する．

フォン・ノイマン安定性解析による確認：
CCD は純中心差分（修正波数 $k^*(kh)$ が実数）であるため，
移流方程式 $\partial\psi/\partial t + u\,\partial\psi/\partial x = 0$ に
前進 Euler 時間積分を組み合わせると，Fourier モード $\psi_i = e^{i\xi i}$ に対して
増幅係数は
\begin{equation}
  |g(\xi)|^2 = 1 + \sigma^2 \bigl[\hat{k}^*(\xi)\bigr]^2 > 1
  \quad (0<\xi<\pi,\ \hat{k}^*(\xi)\neq0), \;\sigma = \frac{u\Delta t}{h} > 0
  \label{eq:ccd_adv_instability}
\end{equation}
ここで $\xi = kh \in (0,\pi]$ は無次元波数，
$\hat{k}^*(\xi) = k^*(\xi)\,h$ は CCD の無次元修正波数（実数；§~\ref{sec:CCD} の Fourier 解析参照），
$\sigma$ は CFL 数である．
ナイキスト点 $\xi=\pi$ では中心型微分がそのモードを感知できず中立となるが，
それ以外の可視波数では $|g|>1$ となる．
したがって純 CCD + 前進 Euler の組合せは，CFL を小さくしても増幅率を1未満にはできない．
TVD-RK3 を使う場合の線形安定性は，後述の虚軸安定域条件で別途判定する．

この不安定性の物理的帰結は \textbf{Gibbs 振動}（$\psi < 0$ や $\psi > 1$
の非物理的値の発生）であり，界面のにじみおよび密度場の不整合として現れる
（第~\ref{sec:intro}章~§~\ref{sec:failure_modes}）．

\subsubsection{Dissipative CCD の採用：フィルタによるスペクトル安定化}
\label{sec:advection_dccd_design}

第~\ref{sec:dissipative_ccd}節で設計した 3 点スペクトルフィルタ
（式~\eqref{eq:dccd_filter_transfer}）を，
CLS 移流フラックス発散の計算に組み込み，高波数側の微分応答を抑える．
この操作だけで中心差分型移流が散逸型に変わるわけではなく，
時間積分の安定域と組み合わせて使用する設計である．

\medskip
\noindent\textbf{移流への適用．}
フラックス場 $f_i = \psi_i u_i$（$x$ 方向）に CCD を適用して微分場 $f'_i$
を求め，その後 Dissipative フィルタを適用する：
\begin{align}
  f'_i &= \left[\frac{\partial(\psi u)}{\partial x}\right]_i
  \quad\text{（CCD 求解，$\Ord{h^6}$ 精度）}
  \label{eq:dccd_adv_ccd} \\[4pt]
  \tilde{F}_i &= f'_i + \varepsilon_d^{\mathrm{adv}}\!\left(f'_{i+1} - 2f'_i + f'_{i-1}\right)
  \label{eq:dccd_adv_filter}
\end{align}
得られた修正フラックス発散 $\tilde{F}_i$ を時間積分（§~\ref{sec:time_int}）の
空間演算子として使用する：$\mathcal{L}(\psi) = -\tilde{F}$
（PDE $\partial\psi/\partial t = -\bnabla\cdot(\psi\mathbf{u})$ の符号に整合）．

\medskip
\noindent\textbf{安定化機構：フィルタ伝達関数による微分応答の低減．}
Fourier モード $\psi_i = e^{i\xi i}$ に対して
$f'_{i+1} - 2f'_i + f'_{i-1} = (e^{i\xi}-2+e^{-i\xi})f'_i = -4\sin^2(\xi/2)\,f'_i$
であるから，フィルタ適用後の有効スペクトルは
$\tilde{F}(\xi) = H(\xi;\varepsilon_d^{\mathrm{adv}})\cdot f'(\xi)$，
$H(\xi) = 1 - 4\varepsilon_d^{\mathrm{adv}}\sin^2(\xi/2) \leq 1$．

この実数係数（$0 \leq H \leq 1$）の乗算は，
中心差分型微分シンボルの大きさを高波数側で小さくする．
ただし，微分シンボルが純虚数である限り，半離散演算子そのものは散逸を持たない．
前進 Euler と組み合わせた場合の増幅係数は
\begin{equation}
  |g(\xi)|^2 = 1 + \sigma^2 H(\xi)^2\bigl[\hat{k}^*(\xi)\bigr]^2
  \label{eq:dccd_adv_amp}
\end{equation}
となり，式~\eqref{eq:dccd_adv_amp} は依然として $|g|^2 > 1$（$\xi > 0$）である．
したがって\textbf{フィルタ単独，または前進 Euler との組合せでは絶対安定性は保証されない}．
$H(\xi) < 1$ によって有効 CFL 数が $\sigma H(\xi) < \sigma$ に縮小し，
高波数（$\xi \to \pi$，$H \to 1-4\varepsilon_d^{\mathrm{adv}}$）では
TVD-RK3 の虚軸安定域に入りやすくなる．
実際の線形安定性は TVD-RK3 との組み合わせで判定する：
TVD-RK3 の虚軸安定条件が
$\sigma\,H(\xi)\,|\hat{k}^*(\xi)| \leq \sqrt{3}$ を満たす範囲では，
線形定係数移流に対して安定域内に収まる．
典型的な二相流パラメータ（$\mathrm{CFL} \leq 0.5$，$\varepsilon_d^{\mathrm{adv}} = 0.05$）のもとで
実用的な安定性が得られることが期待される．
\textbf{本上界は設計目標（未検証）}であり，数値実験による定量検証は
§~\ref{sec:verification} の対象として今後の課題である．
形式的上界は §~\ref{result:dccd_stability} の結果ボックスを参照されたい．

\begin{tcolorbox}[warnbox, title={移流フィルタの散逸強度：S(ψ) 制御を使わない理由}]
\label{warn:adv_filter_uniform}

第~\ref{sec:dissipative_ccd}節の速度・圧力場フィルタでは
スイッチ関数 $S(\psi) = (2\psi-1)^2$ を用いて
\textbf{界面でフィルタを OFF}（$S(1/2) = 0$）にし，曲率計算の精度を保護した．

CLS 移流に同じ $S(\psi)$ を適用すると，Gibbs 振動が最も激しい
$\psi \approx 0.5$（界面）の近傍でフィルタ強度がゼロになり，
安定化が最も必要な場所で無効化されるという\textbf{設計矛盾}が生じる．

\textbf{したがって CLS 移流には一様フィルタ強度を使用する：}
\begin{equation}
  \varepsilon_d^{\mathrm{adv}} = \varepsilon_{d,\max} = 0.05 \quad\text{（全格子点，S(ψ) 制御なし）}
  \label{eq:eps_adv}
\end{equation}
この値のもとでナイキスト波長 $2h$（$\xi=\pi$）での伝達関数は
$H(\pi;\,0.05) = 1 - 4\times 0.05 = 0.80$（微分応答を 20\%\ 低減）；
物理的に重要な低波数成分（$\xi \ll \pi$）への影響は
$H(\xi) \approx 1 - \varepsilon_d^{\mathrm{adv}}\xi^2 \approx 1$（精度劣化 $\Ord{\varepsilon_d\xi^2} \ll 1$）に抑えられる
（式~\eqref{eq:dccd_filter_transfer}，§~\ref{sec:dccd_filter_theory}）．
典型的な CFL $\leq 0.5$ のもとでの実用的安定性は第~\ref{sec:verification}章のベンチマークで検証する．

速度・圧力場フィルタ（適応制御 $\varepsilon_d^{(i)} = \varepsilon_{d,\max}S(\psi_i)$）
との使い分けを表~\ref{box:scheme_roles}に整理する．
\end{tcolorbox}

\begin{tcolorbox}[warnbox, title={TVD-RK3 + DCCD 微分応答制御の線形 CFL 上界（線形理論・未検証）}]
\label{result:dccd_stability}
線形定係数移流に対するフォン・ノイマン解析では，
TVD-RK3 の虚軸安定域 $|z|\le\sqrt{3}$ と
DCCD 適用後の最大修正波数
$\max_\xi H(\xi;\varepsilon_d^{\mathrm{adv}})|\hat{k}^*(\xi)| \approx 1.9$
から，CFL 上界の目安は
\begin{equation}
  \sigma_{\max} = \frac{\sqrt{3}}{1.9} \approx 0.91
  \label{eq:cfl_upper_bound}
\end{equation}
となる．本稿の CFL $\leq 0.5$（式~\eqref{eq:dt_adv}）はこの上界を下回る．
ただし，これは定係数・周期境界・局所凍結係数近似での線形評価であり，
非線形 CLS 移流，値域制限，非一様格子，境界条件を含む実計算の安定性は
§~\ref{sec:verify_cls_advection} の数値検証で確認する．
\end{tcolorbox}

\medskip
\noindent\textbf{質量保存性．}
周期境界のブロック巡回 CCD 系では，CCD で得た一次微分場 $f'_i$ の全点和が
離散的にゼロとなる（§~\ref{sec:dccd_mass_conservation}）．
さらにフィルタ項 $\varepsilon_d^{\mathrm{adv}}(f'_{i+1}-2f'_i+f'_{i-1})$ の周期和も
\[
  \sum_i \varepsilon_d^{\mathrm{adv}}\!\left(f'_{i+1} - 2f'_i + f'_{i-1}\right)
  = \varepsilon_d^{\mathrm{adv}}\!\left(\sum_i f'_{i+1} - 2\sum_i f'_i + \sum_i f'_{i-1}\right) = 0
\]
（添字のシフト対称性より）である．
したがって，周期境界・一様格子・値域制限なしの半離散演算子では，
DCCD フィルタ済み発散の総和はゼロとなる．
\label{sec:dccd_conservation_caveat}%
一方，非周期境界や非一様格子では境界フラックス近似，メトリクス，体積重みの扱いに依存し，
一般には保存形 FVM のような厳密テレスコーピングは保証されない．
また，RK ステージ後の clamp や再初期化は別の非保存誤差を生むため，
実運用では §~\ref{sec:dccd_mass_conservation} の質量補正を併用する
（WENO5 の保存形定式化との比較：付録~\ref{app:weno5}；
定式化§~\ref{app:weno5_formulation}，再構成係数§~\ref{app:weno5_coef}，Lax-Friedrichs 分割§~\ref{app:lf_splitting}）．

DCCD は TVD 性を持たないため，各 RK3 ステージ完了後に値域制限が必要である：
\begin{equation}
  \psi_i \leftarrow \max(0,\,\min(1,\,\psi_i))
  \label{eq:psi_clamp}
\end{equation}
\label{warn:adv_clamp}%
これは $\psi \in [0,1]$ の物理的制約と数値安定性を優先する非線形な制限操作であり，
一般には質量保存形ではない．実運用では clamp 後に
§~\ref{sec:dccd_mass_conservation} の界面重み付き質量補正を適用し，
値域制限で生じた総量誤差を戻す
（完全なアルゴリズムは §~\ref{sec:algo_flow}, Step 1）．

\label{alg:dccd_adv}%
完全な CLS 移流の離散アルゴリズム（TVD-RK3 + DCCD フィルタ）は
§~\ref{sec:algo_flow}（Step 1）にまとめる．

\paragraph{誤差指標の注意：$\psi$ 空間 vs $\phi$ 空間}
\label{par:error_metric_caveat}
CLS 移流の形状誤差を $L_2(\psi - \psi_0)$ で評価する場合，
$\psi$ の遷移層幅 $\varepsilon$ に依存するバイアスが生じる：
界面厚 $\varepsilon$ が大きいほど $\psi$ 差が空間的に広がり，
$L_2(\psi)$ が見かけ上悪化する．
異なる $\varepsilon/h$ 設定間の比較や，
格子収束テストの物理的解釈には，
ロジット逆変換 $\phi = \varepsilon\ln(\psi/(1-\psi))$
で $\phi$ 空間に射影した $L_2(\phi - \phi_0)$ を用いるべきである．
$\phi$ 空間誤差は界面位置の幾何学的ずれを直接反映し，
$\varepsilon$ に依存しない．
本稿の実験結果（§~\ref{sec:verify_cls_advection}）では
$L_2(\psi)$ と $L_2(\phi)$ の両方を報告する．

\begin{tcolorbox}[mybox, title={本稿の空間離散化スキームの役割分担}]
\label{box:scheme_roles}
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{>{\raggedright\arraybackslash}p{2.5cm}>{\raggedright\arraybackslash}p{3.8cm}>{\raggedright\arraybackslash}p{1.5cm}>{\raggedright\arraybackslash}p{4cm}}
\hline
対象 & スキーム & 精度 & 根拠 \\
\hline
CLS 移流（$\psi$） & Dissipative CCD：CCD 微分発散形フラックス + 後段スペクトル散逸（§~\ref{sec:advection_motivation}，§~\ref{sec:cls_stages} Stage A） & フィルタ律速 $\Ord{\varepsilon_d h^2}$（CCD 微分 $\Ord{h^6}$ は後段散逸に吸収） & 線形・コンパクト・低散逸；単独 CCD（散逸なし）は $\psi$ に適用禁止；発散形は近似的（厳密 FVM telescoping は §~\ref{sec:dccd_conservation_caveat} の条件下で破れる）；WENO5 は比較参照（付録~\ref{app:weno5}） \\
運動量移流（$u,v$） & UCCD6（§~\ref{sec:uccd6_def}） & CCD 微分 $\Ord{h^6}$；ハイパー粘性 $\Ord{\sigma h^7}$（次数保存） & 演算子内部散逸；$\omega_2^8$ 選択的 Nyquist（L2/L3 bulk 既定） \\
CLS 圧縮（移流部） & Dissipative CCD（§~\ref{sec:cls_compression}） & CCD 微分 $\Ord{h^6}$；固定フィルタ誤差 $\Ord{\varepsilon_d h^2}$ & 移流と同構成（陽解法；非線形フラックス） \\
CLS 圧縮（拡散部） & CCD-CN（§~\ref{sec:cls_compression}） & $\Ord{h^6}$ & 線形拡散部は A-stable \\
曲率・速度微分 & CCD（§~\ref{sec:CCD}） & $\Ord{h^6}$ & 高精度・コンパクト \\
速度・圧力場ノイズ除去 & 散逸的 CCD 適応制御（§~\ref{sec:dissipative_ccd}） & — & $S(\psi)$ で界面保護 \\
圧力 Poisson & 欠陥補正法 PPE + CCD 勾配（§~\ref{sec:pressure}） & $\Ord{h^5}$--$\Ord{h^7}$† & Balanced-Force 整合性（DC k=3） \\
\hline
\end{tabular}
\end{center}
{\small †\ DC k=3 では Dirichlet $\Ord{h^7}$，Neumann BC $\Ord{h^5}$（§~\ref{sec:verify_split_ppe_dc}）．速度補正の圧力勾配は CCD $\Ord{h^6}$（§~\ref{sec:pressure_accuracy_summary}）．}
\end{tcolorbox}
