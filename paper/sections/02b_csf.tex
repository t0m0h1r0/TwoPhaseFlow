% paper/sections/02b_csf.tex
%
% §2.4  表面張力：界面条件とモデル概要
%   離散化（GFM）の詳細は第III部 §\ref{sec:gfm} で述べる．
%   CSF モデルの完全な定式化は付録~\ref{app:csf_full} を参照．
%
% ── 後方互換ラベル（他章からの参照を維持するため） ──────────────────────────────
\phantomsection\label{eq:young-laplace}%
\phantomsection\label{eq:Heps_def_preview}%
\phantomsection\label{eq:delta_eps_def}%
% ═══════════════════════════════════════════════════════════════════════════════
\subsection{表面張力：界面条件とモデル概要}
\label{sec:csf}
% ═══════════════════════════════════════════════════════════════════════════════

表面張力は界面 $\Gamma$ 上の面力であり，NS 方程式（体積方程式）への組み込みには
界面デルタ関数 $\delta_s$ を用いた特異体積力表現（式~\eqref{eq:csf_intro}，§\ref{sec:two_to_one}）：
\begin{equation*}
  \bm{f}_\sigma = \sigma\kappa\hat{\bm{n}}\,\delta_s(\bm{x})
\end{equation*}
が数学的基盤となる．この連続表現を数値計算で近似する方針として二つの選択肢がある．
標準的な手法は Brackbill \textit{et al.}~\cite{Brackbill1992} による
\textbf{連続表面力（CSF: Continuum Surface Force）モデル}であり，
$\delta_s$ を幅 $O(\varepsilon)$ の平滑化関数で置き換えて体積力
$\bm{f}_\sigma \approx \sigma\kappa\bnabla\psi$ を構成する
（完全な定式化は付録~\ref{app:csf_full}）．
CSF モデルは $O(\varepsilon^2) \approx O(\Delta x^2)$ のモデル誤差を持ち，
空間精度の律速段階となる（§\ref{sec:pressure_accuracy_summary}）．

本稿では，この誤差を根本的に除去するため，
界面を数学的不連続面として扱う \textbf{Ghost Fluid Method (GFM)} \cite{Fedkiw1999} を採用する．
GFM の出発点となる界面圧力ジャンプ条件は Young--Laplace 則より：
\begin{equation}
  [p]_\Gamma \equiv p_1 - p_2 = \frac{\kappa}{We}
  \label{eq:gfm_jump}
\end{equation}
（$p_1$：液相側圧力，$p_2$：気相側圧力；無次元形式）．
式~\eqref{eq:gfm_jump} を PPE 右辺に直接組み込む離散化の詳細は
§\ref{sec:gfm}（第III部）で述べる．

% ═══════════════════════════════════════════════════════════════════════════════
